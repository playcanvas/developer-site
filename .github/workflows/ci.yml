name: CI

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - name: Checkout files
      uses: actions/checkout@v7

    - name: Setup Node.js 22.x
      uses: actions/setup-node@v7
      with:
        node-version: 22.x
        cache: 'npm'

    - name: Install dependencies
      run: npm clean-install --progress=false --no-fund

    - name: Build
      run: npm run build

    # Shared static assets must only be emitted by the default-locale build;
    # a localized build ships just its pages plus the small static-theme/
    # assets (~60 MB total vs ~800 MB when duplication regresses). Guards the
    # DOCUSAURUS_CURRENT_LOCALE-based dedup in docusaurus.config.js against
    # regressing on a Docusaurus upgrade (see issue #880).
    - name: Check static assets are not duplicated per locale
      run: |
        JA_SIZE_MB=$(du -sm build/ja | cut -f1)
        echo "Total build size: $(du -sh build | cut -f1) (ja locale: ${JA_SIZE_MB} MB)"
        if [ "$JA_SIZE_MB" -gt 200 ]; then
          echo "::error::build/ja is ${JA_SIZE_MB} MB - shared static assets are being copied per locale again (see issue #880)"
          exit 1
        fi

  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - name: Checkout files
      uses: actions/checkout@v7

    - name: Setup Node.js 22.x
      uses: actions/setup-node@v7
      with:
        node-version: 22.x
        cache: 'npm'

    - name: Install dependencies
      run: npm clean-install --progress=false --no-fund

    - name: Lint
      run: npm run lint

  # The Web Components tag reference is hand-written, so it can drift from the library without
  # anything failing to build. This diffs every tag page (en and ja) against the library's
  # Custom Elements Manifest at the version the live examples are pinned to: attribute names,
  # types, defaults, enum values and events. The script needs only Node built-ins, so there is
  # no install step.
  web-components-manifest:
    name: Web Components manifest
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
    - name: Checkout files
      uses: actions/checkout@v7

    - name: Setup Node.js 22.x
      uses: actions/setup-node@v7
      with:
        node-version: 22.x

    - name: Check the tag reference against the manifest
      run: npm run check:web-components
