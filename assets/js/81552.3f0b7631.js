"use strict";(globalThis.webpackChunkdeveloper_playcanvas_com=globalThis.webpackChunkdeveloper_playcanvas_com||[]).push([[81552],{73416(t,e,n){n.d(e,{m:()=>a});var r=n(74848),s=n(96540),o=n(10977),i=n(18676);const a=({asset:t,children:e,...n})=>{const a=(0,s.useRef)(null),l=(0,s.useRef)(null),u=(0,o.n)();return(0,s.useLayoutEffect)(()=>{if(u&&t?.resource&&a.current){const e=t.resource.instantiateRenderEntity(null);a.current.addChild(e),l.current=e}return()=>{a.current&&l.current&&(l.current.destroy(),a.current.removeChild(l.current),a.current=null,l.current=null)}},[u,parent,t,t?.resource]),t?.resource?(0,r.jsx)(i.w,{ref:a,...n,children:e}):null}},43025(t,e,n){n.d(e,{O:()=>g});var r=n(9039),s=n(36805),o=n(27351),i=n(63744),a=n(96540),l=n(10977),u=n(53282),c=n(56460);

return n.withOptions=n=>t(f(f({},e),n)),n;function n(t,...n){const r="string"==typeof t?[t]:t.raw,{alignValues:s=!1,escapeSpecialCharacters:o=Array.isArray(t),trimWhitespace:i=!0}=e;let a="";for(let e=0;e<r.length;e++){let t=r[e];if(o&&(t=t.replace(/\\\n[ \t]*/g,"").replace(/\\`/g,"`").replace(/\\\$/g,"$").replace(/\\\{/g,"{")),a+=t,e<n.length){a+=s?m(n[e],a):n[e]}}const l=a.split("\n");let u=null;for(const e of l){const t=e.match(/^(\s+)\S+/);if(t){const e=t[1].length;u=u?Math.min(u,e):e}}if(null!==u){const t=u;a=l.map(e=>" "===e[0]||"\t"===e[0]?e.slice(t):e).join("\n")}return i&&(a=a.trim()),o&&(a=a.replace(/\\n/g,"\n").replace(/\\t/g,"\t").replace(/\\r/g,"\r").replace(/\\v/g,"\v").replace(/\\b/g,"\b").replace(/\\f/g,"\f").replace(/\\0/g,"\0").replace(/\\x([\da-fA-F]{2})/g,(t,e)=>String.fromCharCode(parseInt(e,16))).replace(/\\u\{([\da-fA-F]{1,6})\}/g,(t,e)=>String.fromCodePoint(parseInt(e,16))).replace(/\\u([\da-fA-F]{4})/g,(t,e)=>String.fromCharCode(parseInt(e,16)))),"undefined"!=typeof Bun&&(a=a.replace(/\\u(?:\{([\da-fA-F]{1,6})\}|([\da-fA-F]{4}))/g,(t,e,n)=>{var r;const s=null!==(r=null!=e?e:n)&&void 0!==r?r:"";return String.fromCodePoint(parseInt(s,16))})),a}}({}),p=v;function m(t,e){if("string"!=typeof t||!t.includes("\n"))return t;const n=e.slice(e.lastIndexOf("\n")+1).match(/^(\s+)/);if(n){const e=n[1];return t.replace(/\n/g,`\n${e}`)}return t}const x=new Set;function g(t){const e=(0,l.n)(),{center:n,scale:i,rotation:c,depthWrite:h,type:f,showSkybox:d,...v}=t,m={center:n,scale:i,rotation:c,depthWrite:h,type:f,showSkybox:d},g=(0,u.hE)(v,b),T=(0,u.hE)(m,y),S=(0,a.useRef)(!1);return(0,a.useEffect)(()=>{const t=e.root.getGuid(),n=x.has(t);return x.add(t),S.current=n,n&&(0,u.mc)(p`Multiple \`<Environment/>\` components have been mounted.
                Only the first \`<Environment/>\` component will be used.`),()=>{x.delete(t)}}),(0,a.useEffect)(()=>{if(S.current)return;const t=g.skybox;if(!t)return;const n=Array.isArray(t.resources)&&6===t.resources.length;let s=t.resource;return n||(s=r.S.generateSkyboxCubemap(t.resource)),e.scene.skybox=s,()=>{e?.scene&&(e.scene.skybox=null)}},[S.current,g.skybox?.id]),(0,a.useEffect)(()=>{if(!S.current)return e.scene.envAtlas=g?.envAtlas?.resource??null,()=>{e?.scene&&(e.scene.envAtlas=null)}},[S.current,g.envAtlas?.id]),(0,a.useEffect)(()=>{if(S.current)return;e.scene.exposure=g.exposure??1,e.scene.envAtlas=g.envAtlas?.resource??null,T.rotation&&(e.scene.skyboxRotation=(new s.k).setFromEulerAngles(T.rotation[0],T.rotation[1],T.rotation[2])),T.scale&&e.scene.sky.node.setLocalScale(...T.scale),T.position&&e.scene.sky.node.setLocalPosition(...T.position),T.center&&e.scene.sky.center.set(...T.center),e.scene.sky.type=T.type??o.QNk,e.scene.sky.depthWrite=T.depthWrite??!0,e.scene.skyboxMip=g.skyboxMip??0,e.scene.skyboxLuminance=g.skyboxLuminance??1,e.scene.skyboxIntensity=g.skyboxIntensity??1,e.scene.skyboxHighlightMultiplier=g.skyboxHighlightMultiplier??1;const t=e?.scene?.layers?.getLayerByName("Skybox");return t&&(t.enabled=T.showSkybox??!0),()=>{if(e.scene){e.scene.exposure=1,e.scene.skyboxRotation=(new s.k).setFromEulerAngles(0,0,0),e.scene.sky.node.setLocalScale(1,1,1),e.scene.sky.node.setLocalPosition(0,0,0),e.scene.sky.center.set(0,.05,0),e.scene.sky.type=o.llp,e.scene.sky.depthWrite=!1,e.scene.skyboxMip=0,e.scene.skyboxLuminance=0,e.scene.skyboxIntensity=1,e.scene.skyboxHighlightMultiplier=1;const t=e?.scene?.layers?.getLayerByName("Skybox");t&&(t.enabled=!0)}}},[S.current,g.exposure,T.type,T.depthWrite,T.showSkybox,g.skyboxMip,g.skyboxLuminance,g.skyboxIntensity,g.skyboxHighlightMultiplier,`scale-${T.scale?.join("-")}`,`rotation-${T.rotation?.join("-")}`,`center-${T.center?.join("-")}`]),null}const y=(0,u.xE)("Sky",()=>new i.m((0,u.s_)().scene),t=>t.resetSkyMesh()),b=(0,u.xE)("Scene",()=>(0,u.s_)().scene);y.schema={...y.schema,scale:{validate:t=>Array.isArray(t)&&3===t.length&&t.every(t=>"number"==typeof t),errorMsg:t=>`Expected an array of 3 numbers, got \`${typeof t}\``,default:[100,100,100]},rotation:{validate:t=>Array.isArray(t)&&3===t.length&&t.every(t=>"number"==typeof t),errorMsg:t=>`Expected an array of 3 numbers, got \`${typeof t}\``,default:[0,0,0]},position:{validate:t=>Array.isArray(t)&&3===t.length&&t.every(t=>"number"==typeof t),errorMsg:t=>`Expected an array of 3 numbers, got \`${typeof t}\``,default:[0,0,0]},center:{validate:t=>Array.isArray(t)&&3===t.length&&t.every(t=>"number"==typeof t),errorMsg:t=>`Expected an array of 3 numbers, got \`${typeof t}\``,default:[0,.05,0]},showSkybox:{validate:t=>"boolean"==typeof t,errorMsg:t=>`Expected a boolean, got \`${typeof t}\``,default:!0}},b.schema={...b.schema,envAtlas:{validate:t=>t instanceof c.V&&"texture"===t.type,errorMsg:t=>`Expected a \`Asset\` instance, got \`${typeof t}\``,default:null},skybox:{validate:t=>t instanceof c.V&&"texture"===t.type,errorMsg:t=>`Expected a \`Asset\` instance, got \`${typeof t}\``,default:null}}},19748(t,e,n){n.d(e,{L:()=>a,v:()=>i});var r=n(18881),s=n(71586),o=n(53282);const i=t=>{const e=(0,o.hE)(t,a);return(0,r.x)("light",e,a.schema),null},a=(0,o.xE)("Light",()=>new s.w("mock-light",(0,o.s_)()).addComponent("light"),t=>t.system.destroy(),{apiName:"LightComponent"});a.schema={...a.schema,type:{validate:t=>"string"==typeof t&&["directional","omni","spot"].includes(t),errorMsg:t=>`Invalid value for prop "type": ${t}. Expected one of: "directional", "omni", "spot".`,default:"directional"}}},16230(t,e,n){n.d(e,{de:()=>f,dw:()=>c});var r=n(74848),s=n(18881),o=n(73416),i=n(71586),a=n(56460),l=n(53282);const u=t=>((0,s.x)("render",t,f.schema),null),c=t=>{const e=(0,l.cF)(t,f);return"asset"!==e.type||e.asset?"container"===e.asset?.type?(0,r.jsx)(o.m,{asset:e.asset,children:e.children}):(0,r.jsx)(u,{...e}):null},h=["asset","box","capsule","cone","cylinder","plane","sphere","torus"],f=(0,l.xE)("Render",()=>new i.w("mock-render",(0,l.s_)()).addComponent("render"),t=>t.system.destroy(),{apiName:"RenderComponent"});f.schema={...f.schema,children:{validate:t=>"object"==typeof t&&null!==t,errorMsg:t=>`Invalid value for prop "children": ${t}. Expected an object.`,default:void 0},asset:{validate:t=>!t||t instanceof a.V,errorMsg:t=>`Invalid value for prop "asset": ${t}. Expected an Asset.`,default:void 0},type:{validate:t=>"string"==typeof t&&h.includes(t),errorMsg:t=>`Invalid value for prop "type": ${t}. Expected one of: "${h.join('", "')}".`,default:"box"}}},82473(t,e,n){n.d(e,{P:()=>nt});var r=n(48355),s=n(85169),o=n(59752),i=n(91622);const a="none";var l=n(27351),u=n(67248),c=n(55485),h=n(27173),f=n(63138),d=n(58333),v=n(84140),p=n(76178),m=n(46659),x=n(80290);class g extends p.s{setSourceTexture(t){this._sourceTexture=t,this.options.resizeSource=t}execute(){this.sourceTextureId.setValue(this.sourceTexture),this.premultiplyTexture&&this.premultiplyTextureId.setValue(this.premultiplyTexture),this.sourceInvResolutionValue[0]=1/this.sourceTexture.width,this.sourceInvResolutionValue[1]=1/this.sourceTexture.height,this.sourceInvResolutionId.setValue(this.sourceInvResolutionValue),super.execute()}constructor(t,e,n={}){super(t),this.sourceTexture=e,this.premultiplyTexture=n.premultiplyTexture,x.W.get(t,i.BzK).set("downsamplePS","\nuniform sampler2D sourceTexture;\nuniform vec2 sourceInvResolution;\nvarying vec2 uv0;\n#ifdef PREMULTIPLY\n\tuniform sampler2D premultiplyTexture;\n#endif\nvoid main()\n{\n\tvec3 e = texture2D (sourceTexture, uv0).rgb;\n\t#ifdef BOXFILTER\n\t\tvec3 value = e;\n\t\t#ifdef PREMULTIPLY\n\t\t\tfloat premultiply = texture2D(premultiplyTexture, uv0).{PREMULTIPLY_SRC_CHANNEL};\n\t\t\tvalue *= vec3(premultiply);\n\t\t#endif\n\t#else\n\t\tfloat x = sourceInvResolution.x;\n\t\tfloat y = sourceInvResolution.y;\n\t\tvec3 a = texture2D(sourceTexture, vec2 (uv0.x - 2.0 * x, uv0.y + 2.0 * y)).rgb;\n\t\tvec3 b = texture2D(sourceTexture, vec2 (uv0.x,\t\t   uv0.y + 2.0 * y)).rgb;\n\t\tvec3 c = texture2D(sourceTexture, vec2 (uv0.x + 2.0 * x, uv0.y + 2.0 * y)).rgb;\n\t\tvec3 d = texture2D(sourceTexture, vec2 (uv0.x - 2.0 * x, uv0.y)).rgb;\n\t\tvec3 f = texture2D(sourceTexture, vec2 (uv0.x + 2.0 * x, uv0.y)).rgb;\n\t\tvec3 g = texture2D(sourceTexture, vec2 (uv0.x - 2.0 * x, uv0.y - 2.0 * y)).rgb;\n\t\tvec3 h = texture2D(sourceTexture, vec2 (uv0.x,\t\t   uv0.y - 2.0 * y)).rgb;\n\t\tvec3 i = texture2D(sourceTexture, vec2 (uv0.x + 2.0 * x, uv0.y - 2.0 * y)).rgb;\n\t\tvec3 j = texture2D(sourceTexture, vec2 (uv0.x - x, uv0.y + y)).rgb;\n\t\tvec3 k = texture2D(sourceTexture, vec2 (uv0.x + x, uv0.y + y)).rgb;\n\t\tvec3 l = texture2D(sourceTexture, vec2 (uv0.x - x, uv0.y - y)).rgb;\n\t\tvec3 m = texture2D(sourceTexture, vec2 (uv0.x + x, uv0.y - y)).rgb;\n\t\tvec3 value = e * 0.125;\n\t\tvalue += (a + c + g + i) * 0.03125;\n\t\tvalue += (b + d + f + h) * 0.0625;\n\t\tvalue += (j + k + l + m) * 0.125;\n\t#endif\n\t#ifdef REMOVE_INVALID\n\t\tvalue = max(value, vec3(0.0));\n\t#endif\n\tgl_FragColor = vec4(value, 1.0);\n}\n"),x.W.get(t,i.g80).set("downsamplePS","\nvar sourceTexture: texture_2d<f32>;\nvar sourceTextureSampler: sampler;\nuniform sourceInvResolution: vec2f;\nvarying uv0: vec2f;\n#ifdef PREMULTIPLY\n\tvar premultiplyTexture: texture_2d<f32>;\n\tvar premultiplyTextureSampler: sampler;\n#endif\n@fragment\nfn fragmentMain(input: FragmentInput) -> FragmentOutput {\n\tvar output: FragmentOutput;\n\tlet e: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, input.uv0).rgb);\n\t#ifdef BOXFILTER\n\t\tvar value: half3 = e;\n\t\t#ifdef PREMULTIPLY\n\t\t\tlet premultiply: half = half(textureSample(premultiplyTexture, premultiplyTextureSampler, input.uv0).{PREMULTIPLY_SRC_CHANNEL});\n\t\t\tvalue *= premultiply;\n\t\t#endif\n\t#else\n\t\tlet x: f32 = uniform.sourceInvResolution.x;\n\t\tlet y: f32 = uniform.sourceInvResolution.y;\n\t\tlet a: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x - 2.0 * x, input.uv0.y + 2.0 * y)).rgb);\n\t\tlet b: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x,\t\t   input.uv0.y + 2.0 * y)).rgb);\n\t\tlet c: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x + 2.0 * x, input.uv0.y + 2.0 * y)).rgb);\n\t\tlet d: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x - 2.0 * x, input.uv0.y)).rgb);\n\t\tlet f: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x + 2.0 * x, input.uv0.y)).rgb);\n\t\tlet g: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x - 2.0 * x, input.uv0.y - 2.0 * y)).rgb);\n\t\tlet h: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x,\t\t   input.uv0.y - 2.0 * y)).rgb);\n\t\tlet i: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x + 2.0 * x, input.uv0.y - 2.0 * y)).rgb);\n\t\tlet j: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x - x, input.uv0.y + y)).rgb);\n\t\tlet k: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x + x, input.uv0.y + y)).rgb);\n\t\tlet l: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x - x, input.uv0.y - y)).rgb);\n\t\tlet m: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x + x, input.uv0.y - y)).rgb);\n\t\tvar value: half3 = e * half(0.125);\n\t\tvalue += (a + c + g + i) * half(0.03125);\n\t\tvalue += (b + d + f + h) * half(0.0625);\n\t\tvalue += (j + k + l + m) * half(0.125);\n\t#endif\n\t#ifdef REMOVE_INVALID\n\t\tvalue = max(value, half3(0.0));\n\t#endif\n\toutput.color = vec4f(vec3f(value), 1.0);\n\treturn output;\n}\n");const r=n.boxFilter??!1,s=`${r?"Box":""}-${n.premultiplyTexture?"Premultiply":""}-${n.premultiplySrcChannel??""}-${n.removeInvalid?"RemoveInvalid":""}`,o=new Map;r&&o.set("BOXFILTER",""),n.premultiplyTexture&&o.set("PREMULTIPLY",""),n.removeInvalid&&o.set("REMOVE_INVALID",""),o.set("{PREMULTIPLY_SRC_CHANNEL}",n.premultiplySrcChannel??"x"),this.shader=m.lo.createShader(t,{uniqueName:`DownSampleShader:${s}`,attributes:{aPosition:i.JYe},vertexChunk:"quadVS",fragmentChunk:"downsamplePS",fragmentDefines:o}),this.sourceTextureId=t.scope.resolve("sourceTexture"),this.premultiplyTextureId=t.scope.resolve("premultiplyTexture"),this.sourceInvResolutionId=t.scope.resolve("sourceInvResolution"),this.sourceInvResolutionValue=new Float32Array(2)}}class y extends p.s{execute(){this.sourceTextureId.setValue(this.sourceTexture),this.sourceInvResolutionValue[0]=1/this.sourceTexture.width,this.sourceInvResolutionValue[1]=1/this.sourceTexture.height,this.sourceInvResolutionId.setValue(this.sourceInvResolutionValue),super.execute()}constructor(t,e){super(t),this.sourceTexture=e,x.W.get(t,i.BzK).set("upsamplePS","\n\tuniform sampler2D sourceTexture;\n\tuniform vec2 sourceInvResolution;\n\tvarying vec2 uv0;\n\tvoid main()\n\t{\n\t\tfloat x = sourceInvResolution.x;\n\t\tfloat y = sourceInvResolution.y;\n\t\tvec3 a = texture2D (sourceTexture, vec2 (uv0.x - x, uv0.y + y)).rgb;\n\t\tvec3 b = texture2D (sourceTexture, vec2 (uv0.x,\t uv0.y + y)).rgb;\n\t\tvec3 c = texture2D (sourceTexture, vec2 (uv0.x + x, uv0.y + y)).rgb;\n\t\tvec3 d = texture2D (sourceTexture, vec2 (uv0.x - x, uv0.y)).rgb;\n\t\tvec3 e = texture2D (sourceTexture, vec2 (uv0.x,\t uv0.y)).rgb;\n\t\tvec3 f = texture2D (sourceTexture, vec2 (uv0.x + x, uv0.y)).rgb;\n\t\tvec3 g = texture2D (sourceTexture, vec2 (uv0.x - x, uv0.y - y)).rgb;\n\t\tvec3 h = texture2D (sourceTexture, vec2 (uv0.x,\t uv0.y - y)).rgb;\n\t\tvec3 i = texture2D (sourceTexture, vec2 (uv0.x + x, uv0.y - y)).rgb;\n\t\tvec3 value = e * 0.25;\n\t\tvalue += (b + d + f + h) * 0.125;\n\t\tvalue += (a + c + g + i) * 0.0625;\n\t\tgl_FragColor = vec4(value, 1.0);\n\t}\n"),x.W.get(t,i.g80).set("upsamplePS","\n\tvar sourceTexture: texture_2d<f32>;\n\tvar sourceTextureSampler: sampler;\n\tuniform sourceInvResolution: vec2f;\n\tvarying uv0: vec2f;\n\t@fragment\n\tfn fragmentMain(input: FragmentInput) -> FragmentOutput {\n\t\tvar output: FragmentOutput;\n\t\tlet x: f32 = uniform.sourceInvResolution.x;\n\t\tlet y: f32 = uniform.sourceInvResolution.y;\n\t\tlet a: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x - x, input.uv0.y + y)).rgb);\n\t\tlet b: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x,\t input.uv0.y + y)).rgb);\n\t\tlet c: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x + x, input.uv0.y + y)).rgb);\n\t\tlet d: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x - x, input.uv0.y)).rgb);\n\t\tlet e: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x,\t input.uv0.y)).rgb);\n\t\tlet f: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x + x, input.uv0.y)).rgb);\n\t\tlet g: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x - x, input.uv0.y - y)).rgb);\n\t\tlet h: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x,\t input.uv0.y - y)).rgb);\n\t\tlet i: half3 = half3(textureSample(sourceTexture, sourceTextureSampler, vec2f(input.uv0.x + x, input.uv0.y - y)).rgb);\n\t\tvar value: half3 = e * half(0.25);\n\t\tvalue += (b + d + f + h) * half(0.125);\n\t\tvalue += (a + c + g + i) * half(0.0625);\n\t\toutput.color = vec4f(vec3f(value), 1.0);\n\t\treturn output;\n\t}\n"),this.shader=m.lo.createShader(t,{uniqueName:"UpSampleShader",attributes:{aPosition:i.JYe},vertexChunk:"quadVS",fragmentChunk:"upsamplePS"}),this.sourceTextureId=t.scope.resolve("sourceTexture"),this.sourceInvResolutionId=t.scope.resolve("sourceInvResolution"),this.sourceInvResolutionValue=new Float32Array(2)}}class b extends c.A{destroy(){this.destroyRenderPasses(),this.destroyRenderTargets()}destroyRenderTargets(t=0){for(let e=t;e<this.renderTargets.length;e++){const t=this.renderTargets[e];t.destroyTextureBuffers(),t.destroy()}this.renderTargets.length=0}destroyRenderPasses(){for(let t=0;t<this.beforePasses.length;t++)this.beforePasses[t].destroy();this.beforePasses.length=0}createRenderTarget(t){return new d.O({depth:!1,colorBuffer:new u.g(this.device,{name:`BloomTexture${t}`,width:1,height:1,format:this.textureFormat,mipmaps:!1,minFilter:i.DrA,magFilter:i.DrA,addressU:i.vq6,addressV:i.vq6})})}createRenderTargets(t){for(let e=0;e<t;e++){const t=0===e?this.bloomRenderTarget:this.createRenderTarget(e);this.renderTargets.push(t)}}calcMipLevels(t,e,n){const r=Math.min(t,e);return Math.floor(Math.log2(r)-Math.log2(n))}createRenderPasses(t){const e=this.device;let n=this._sourceTexture;for(let s=0;s<t;s++){const t=new g(e,n),o=this.renderTargets[s];t.init(o,{resizeSource:n,scaleX:.5,scaleY:.5}),t.setClearColor(r.Q.BLACK),this.beforePasses.push(t),n=o.colorBuffer}n=this.renderTargets[t-1].colorBuffer;for(let r=t-2;r>=0;r--){const t=new y(e,n),s=this.renderTargets[r];t.init(s),t.blendState=v.t.ADDBLEND,this.beforePasses.push(t),n=s.colorBuffer}}onDisable(){this.renderTargets[0]?.resize(1,1),this.destroyRenderPasses(),this.destroyRenderTargets(1)}frameUpdate(){super.frameUpdate();const t=this.calcMipLevels(this._sourceTexture.width,this._sourceTexture.height,1),e=o.D.clamp(t,1,this.blurLevel);this.renderTargets.length!==e&&(this.destroyRenderPasses(),this.destroyRenderTargets(1),this.createRenderTargets(e),this.createRenderPasses(e))}constructor(t,e,n){super(t),this.blurLevel=16,this.renderTargets=[],this._sourceTexture=e,this.textureFormat=n,this.bloomRenderTarget=this.createRenderTarget(0),this.bloomTexture=this.bloomRenderTarget.colorBuffer}}var T=n(88525);const S={composePS:'\n\t#include "tonemappingPS"\n\t#include "gammaPS"\n\tvarying vec2 uv0;\n\tuniform sampler2D sceneTexture;\n\tuniform vec2 sceneTextureInvRes;\n\t#include "composeBloomPS"\n\t#include "composeDofPS"\n\t#include "composeSsaoPS"\n\t#include "composeGradingPS"\n\t#include "composeColorEnhancePS"\n\t#include "composeVignettePS"\n\t#include "composeFringingPS"\n\t#include "composeCasPS"\n\t#include "composeColorLutPS"\n\t#include "composeDeclarationsPS"\n\tvoid main() {\n\t\t#include "composeMainStartPS"\n\t\tvec2 uv = uv0;\n\t\tvec4 scene = texture2DLod(sceneTexture, uv, 0.0);\n\t\tvec3 result = scene.rgb;\n\t\t#ifdef CAS\n\t\t\tresult = applyCas(result, uv, sharpness);\n\t\t#endif\n\t\t#ifdef DOF\n\t\t\tresult = applyDof(result, uv0);\n\t\t#endif\n\t\t#ifdef SSAO_TEXTURE\n\t\t\tresult = applySsao(result, uv0);\n\t\t#endif\n\t\t#ifdef FRINGING\n\t\t\tresult = applyFringing(result, uv);\n\t\t#endif\n\t\t#ifdef BLOOM\n\t\t\tresult = applyBloom(result, uv0);\n\t\t#endif\n\t\t#ifdef COLOR_ENHANCE\n\t\t\tresult = applyColorEnhance(result);\n\t\t#endif\n\t\t#ifdef GRADING\n\t\t\tresult = applyGrading(result);\n\t\t#endif\n\t\tresult = toneMap(max(vec3(0.0), result));\n\t\t#ifdef COLOR_LUT\n\t\t\tresult = applyColorLUT(result);\n\t\t#endif\n\t\t#ifdef VIGNETTE\n\t\t\tresult = applyVignette(result, uv);\n\t\t#endif\n\t\t#include "composeMainEndPS"\n\t\t#ifdef DEBUG_COMPOSE\n\t\t\t#if DEBUG_COMPOSE == scene\n\t\t\t\tresult = scene.rgb;\n\t\t\t#elif defined(BLOOM) && DEBUG_COMPOSE == bloom\n\t\t\t\tresult = dBloom * bloomIntensity;\n\t\t\t#elif defined(DOF) && DEBUG_COMPOSE == dofcoc\n\t\t\t\tresult = vec3(dCoc, 0.0);\n\t\t\t#elif defined(DOF) && DEBUG_COMPOSE == dofblur\n\t\t\t\tresult = dBlur;\n\t\t\t#elif defined(SSAO_TEXTURE) && DEBUG_COMPOSE == ssao\n\t\t\t\tresult = vec3(dSsao);\n\t\t\t#elif defined(VIGNETTE) && DEBUG_COMPOSE == vignette\n\t\t\t\tresult = vec3(dVignette);\n\t\t\t#endif\n\t\t#endif\n\t\tresult = gammaCorrectOutput(result);\n\t\tgl_FragColor = vec4(result, scene.a);\n\t}\n',composeBloomPS:"\n\t#ifdef BLOOM\n\t\tuniform sampler2D bloomTexture;\n\t\tuniform float bloomIntensity;\n\t\t\n\t\tvec3 dBloom;\n\t\t\n\t\tvec3 applyBloom(vec3 color, vec2 uv) {\n\t\t\tdBloom = texture2DLod(bloomTexture, uv, 0.0).rgb;\n\t\t\treturn color + dBloom * bloomIntensity;\n\t\t}\n\t#endif\n",composeDofPS:"\n\t#ifdef DOF\n\t\tuniform sampler2D cocTexture;\n\t\tuniform sampler2D blurTexture;\n\t\t\n\t\tvec2 dCoc;\n\t\tvec3 dBlur;\n\t\tvec3 getDofBlur(vec2 uv) {\n\t\t\tdCoc = texture2DLod(cocTexture, uv, 0.0).rg;\n\t\t\t#if DOF_UPSCALE\n\t\t\t\tvec2 blurTexelSize = 1.0 / vec2(textureSize(blurTexture, 0));\n\t\t\t\tvec3 bilinearBlur = vec3(0.0);\n\t\t\t\tfloat totalWeight = 0.0;\n\t\t\t\tfor (int i = -1; i <= 1; i++) {\n\t\t\t\t\tfor (int j = -1; j <= 1; j++) {\n\t\t\t\t\t\tvec2 offset = vec2(i, j) * blurTexelSize;\n\t\t\t\t\t\tvec2 cocSample = texture2DLod(cocTexture, uv + offset, 0.0).rg;\n\t\t\t\t\t\tvec3 blurSample = texture2DLod(blurTexture, uv + offset, 0.0).rgb;\n\t\t\t\t\t\tfloat cocWeight = clamp(cocSample.r + cocSample.g, 0.0, 1.0);\n\t\t\t\t\t\tbilinearBlur += blurSample * cocWeight;\n\t\t\t\t\t\ttotalWeight += cocWeight;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\tif (totalWeight > 0.0) {\n\t\t\t\t\tbilinearBlur /= totalWeight;\n\t\t\t\t}\n\t\t\t\tdBlur = bilinearBlur;\n\t\t\t\treturn bilinearBlur;\n\t\t\t#else\n\t\t\t\tdBlur = texture2DLod(blurTexture, uv, 0.0).rgb;\n\t\t\t\treturn dBlur;\n\t\t\t#endif\n\t\t}\n\t\tvec3 applyDof(vec3 color, vec2 uv) {\n\t\t\tvec3 blur = getDofBlur(uv);\n\t\t\treturn mix(color, blur, dCoc.r + dCoc.g);\n\t\t}\n\t#endif\n",composeSsaoPS:"\n\t#ifdef SSAO\n\t\t#define SSAO_TEXTURE\n\t#endif\n\t#if DEBUG_COMPOSE == ssao\n\t\t#define SSAO_TEXTURE\n\t#endif\n\t#ifdef SSAO_TEXTURE\n\t\tuniform sampler2D ssaoTexture;\n\t\t\n\t\tfloat dSsao;\n\t\t\n\t\tvec3 applySsao(vec3 color, vec2 uv) {\n\t\t\tdSsao = texture2DLod(ssaoTexture, uv, 0.0).r;\n\t\t\t\n\t\t\t#ifdef SSAO\n\t\t\t\treturn color * dSsao;\n\t\t\t#else\n\t\t\t\treturn color;\n\t\t\t#endif\n\t\t}\n\t#endif\n",composeGradingPS:"\n\t#ifdef GRADING\n\t\tuniform vec3 brightnessContrastSaturation;\n\t\tuniform vec3 tint;\n\t\tvec3 colorGradingHDR(vec3 color, float brt, float sat, float con) {\n\t\t\tcolor *= tint;\n\t\t\tcolor = color * brt;\n\t\t\tfloat grey = dot(color, vec3(0.3, 0.59, 0.11));\n\t\t\tgrey = grey / max(1.0, max(color.r, max(color.g, color.b)));\n\t\t\tcolor = mix(vec3(grey), color, sat);\n\t\t\treturn mix(vec3(0.5), color, con);\n\t\t}\n\t\tvec3 applyGrading(vec3 color) {\n\t\t\treturn colorGradingHDR(color, \n\t\t\t\tbrightnessContrastSaturation.x, \n\t\t\t\tbrightnessContrastSaturation.z, \n\t\t\t\tbrightnessContrastSaturation.y);\n\t\t}\n\t#endif\n",composeColorEnhancePS:"\n\t#ifdef COLOR_ENHANCE\n\t\tuniform vec4 colorEnhanceParams;\n\t\tuniform float colorEnhanceMidtones;\n\t\tvec3 applyColorEnhance(vec3 color) {\n\t\t\tfloat maxChannel = max(color.r, max(color.g, color.b));\n\t\t\tfloat lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n\t\t\tif (colorEnhanceParams.x != 0.0 || colorEnhanceParams.y != 0.0) {\n\t\t\t\tfloat logLum = log2(max(lum, 0.001)) / 10.0 + 0.5;\n\t\t\t\tlogLum = clamp(logLum, 0.0, 1.0);\n\t\t\t\tfloat shadowWeight = pow(1.0 - logLum, 2.0);\n\t\t\t\tfloat highlightWeight = pow(logLum, 2.0);\n\t\t\t\tcolor *= pow(2.0, colorEnhanceParams.x * shadowWeight);\n\t\t\t\tcolor *= pow(2.0, colorEnhanceParams.y * highlightWeight);\n\t\t\t}\n\t\t\tif (colorEnhanceMidtones != 0.0) {\n\t\t\t\tconst float pivot = 0.18;\n\t\t\t\tconst float widthStops = 1.25;\n\t\t\t\tconst float maxStops = 2.0;\n\t\t\t\tfloat y = max(dot(color, vec3(0.2126, 0.7152, 0.0722)), 1e-6);\n\t\t\t\tfloat d = log2(y / pivot);\n\t\t\t\tfloat w = exp(-(d * d) / (2.0 * widthStops * widthStops));\n\t\t\t\tfloat stops = colorEnhanceMidtones * maxStops * w;\n\t\t\t\tcolor *= exp2(stops);\n\t\t\t}\n\t\t\tif (colorEnhanceParams.z != 0.0) {\n\t\t\t\tfloat minChannel = min(color.r, min(color.g, color.b));\n\t\t\t\tmaxChannel = max(color.r, max(color.g, color.b));\n\t\t\t\tfloat sat = (maxChannel - minChannel) / max(maxChannel, 0.001);\n\t\t\t\tlum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n\t\t\t\tfloat normalizedLum = lum / max(1.0, maxChannel);\n\t\t\t\tvec3 grey = vec3(normalizedLum) * maxChannel;\n\t\t\t\tfloat satBoost = colorEnhanceParams.z * (1.0 - sat);\n\t\t\t\tcolor = mix(grey, color, 1.0 + satBoost);\n\t\t\t}\n\t\t\tif (colorEnhanceParams.w != 0.0) {\n\t\t\t\tmaxChannel = max(color.r, max(color.g, color.b));\n\t\t\t\tfloat scale = max(1.0, maxChannel);\n\t\t\t\tvec3 normalized = color / scale;\n\t\t\t\tfloat darkChannel = min(normalized.r, min(normalized.g, normalized.b));\n\t\t\t\tfloat atmosphericLight = 0.95;\n\t\t\t\tfloat t = 1.0 - colorEnhanceParams.w * darkChannel / atmosphericLight;\n\t\t\t\tt = max(t, 0.1);\n\t\t\t\tvec3 dehazed = (normalized - atmosphericLight) / t + atmosphericLight;\n\t\t\t\tcolor = dehazed * scale;\n\t\t\t}\n\t\t\treturn max(vec3(0.0), color);\n\t\t}\n\t#endif\n",composeVignettePS:"\n\t#ifdef VIGNETTE\n\t\tuniform vec4 vignetterParams;\n\t\tuniform vec3 vignetteColor;\n\t\t\n\t\tfloat dVignette;\n\t\t\n\t\tfloat calcVignette(vec2 uv) {\n\t\t\tfloat inner = vignetterParams.x;\n\t\t\tfloat outer = vignetterParams.y;\n\t\t\tfloat curvature = vignetterParams.z;\n\t\t\tfloat intensity = vignetterParams.w;\n\t\t\tvec2 curve = pow(abs(uv * 2.0 -1.0), vec2(1.0 / curvature));\n\t\t\tfloat edge = pow(length(curve), curvature);\n\t\t\tdVignette = 1.0 - intensity * smoothstep(inner, outer, edge);\n\t\t\treturn dVignette;\n\t\t}\n\t\tvec3 applyVignette(vec3 color, vec2 uv) {\n\t\t\treturn mix(vignetteColor, color, calcVignette(uv));\n\t\t}\n\t#endif\n",composeFringingPS:"\n\t#ifdef FRINGING\n\t\tuniform float fringingIntensity;\n\t\tvec3 applyFringing(vec3 color, vec2 uv) {\n\t\t\tvec2 centerDistance = uv - 0.5;\n\t\t\tvec2 offset = fringingIntensity * centerDistance * centerDistance;\n\t\t\tcolor.r = texture2D(sceneTexture, uv - offset).r;\n\t\t\tcolor.b = texture2D(sceneTexture, uv + offset).b;\n\t\t\treturn color;\n\t\t}\n\t#endif\n",composeCasPS:"\n\t#ifdef CAS\n\t\tuniform float sharpness;\n\t\t#ifdef CAS_HDR\n\t\t\tfloat maxComponent(float x, float y, float z) { return max(x, max(y, z)); }\n\t\t\tvec3 toSDR(vec3 c) { return c / (1.0 + maxComponent(c.r, c.g, c.b)); }\n\t\t\tvec3 toHDR(vec3 c) { return c / max(1.0 - maxComponent(c.r, c.g, c.b), 1e-4); }\n\t\t#else\n\t\t\tvec3 toSDR(vec3 c) { return c; }\n\t\t\tvec3 toHDR(vec3 c) { return c; }\n\t\t#endif\n\t\tvec3 applyCas(vec3 color, vec2 uv, float sharpness) {\n\t\t\tfloat x = sceneTextureInvRes.x;\n\t\t\tfloat y = sceneTextureInvRes.y;\n\t\t\tvec3 a = toSDR(texture2DLod(sceneTexture, uv + vec2(0.0, -y), 0.0).rgb);\n\t\t\tvec3 b = toSDR(texture2DLod(sceneTexture, uv + vec2(-x, 0.0), 0.0).rgb);\n\t\t\tvec3 c = toSDR(color.rgb);\n\t\t\tvec3 d = toSDR(texture2DLod(sceneTexture, uv + vec2(x, 0.0), 0.0).rgb);\n\t\t\tvec3 e = toSDR(texture2DLod(sceneTexture, uv + vec2(0.0, y), 0.0).rgb);\n\t\t\tfloat min_g = min(a.g, min(b.g, min(c.g, min(d.g, e.g))));\n\t\t\tfloat max_g = max(a.g, max(b.g, max(c.g, max(d.g, e.g))));\n\t\t\tfloat sharpening_amount = sqrt(min(1.0 - max_g, min_g) / max(max_g, 1e-4));\n\t\t\tfloat w = sharpening_amount * sharpness;\n\t\t\tvec3 res = (w * (a + b + d + e) + c) / (4.0 * w + 1.0);\n\t\t\tres = max(res, 0.0);\n\t\t\treturn toHDR(res);\n\t\t}\n\t#endif\n",composeColorLutPS:"\n\t#ifdef COLOR_LUT\n\t\tuniform sampler2D colorLUT;\n\t\tuniform vec4 colorLUTParams;\n\t\tvec3 applyColorLUT(vec3 color) {\n\t\t\tvec3 c = clamp(color, 0.0, 1.0);\n\t\t\tfloat width = colorLUTParams.x;\n\t\t\tfloat height = colorLUTParams.y;\n\t\t\tfloat maxColor = colorLUTParams.z;\n\t\t\tfloat cell = c.b * maxColor;\n\t\t\tfloat cell_l = floor(cell);\n\t\t\tfloat cell_h = ceil(cell);\n\t\t\tfloat half_px_x = 0.5 / width;\n\t\t\tfloat half_px_y = 0.5 / height;\n\t\t\tfloat r_offset = half_px_x + c.r / height * (maxColor / height);\n\t\t\tfloat g_offset = half_px_y + c.g * (maxColor / height);\n\t\t\tvec2 uv_l = vec2(cell_l / height + r_offset, g_offset);\n\t\t\tvec2 uv_h = vec2(cell_h / height + r_offset, g_offset);\n\t\t\tvec3 color_l = texture2DLod(colorLUT, uv_l, 0.0).rgb;\n\t\t\tvec3 color_h = texture2DLod(colorLUT, uv_h, 0.0).rgb;\n\t\t\tvec3 lutColor = mix(color_l, color_h, fract(cell));\n\t\t\treturn mix(color, lutColor, colorLUTParams.w);\n\t\t}\n\t#endif\n",composeDeclarationsPS:"",composeMainStartPS:"",composeMainEndPS:""};const P={composePS:'\n\t#include "tonemappingPS"\n\t#include "gammaPS"\n\tvarying uv0: vec2f;\n\tvar sceneTexture: texture_2d<f32>;\n\tvar sceneTextureSampler: sampler;\n\tuniform sceneTextureInvRes: vec2f;\n\t#include "composeBloomPS"\n\t#include "composeDofPS"\n\t#include "composeSsaoPS"\n\t#include "composeGradingPS"\n\t#include "composeColorEnhancePS"\n\t#include "composeVignettePS"\n\t#include "composeFringingPS"\n\t#include "composeCasPS"\n\t#include "composeColorLutPS"\n\t#include "composeDeclarationsPS"\n\t@fragment\n\tfn fragmentMain(input: FragmentInput) -> FragmentOutput {\n\t\t#include "composeMainStartPS"\n\t\tvar output: FragmentOutput;\n\t\tvar uv = uv0;\n\t\tlet scene = textureSampleLevel(sceneTexture, sceneTextureSampler, uv, 0.0);\n\t\tvar result = scene.rgb;\n\t\t#ifdef CAS\n\t\t\tresult = applyCas(result, uv, uniform.sharpness);\n\t\t#endif\n\t\t#ifdef DOF\n\t\t\tresult = applyDof(result, uv0);\n\t\t#endif\n\t\t#ifdef SSAO_TEXTURE\n\t\t\tresult = applySsao(result, uv0);\n\t\t#endif\n\t\t#ifdef FRINGING\n\t\t\tresult = applyFringing(result, uv);\n\t\t#endif\n\t\t#ifdef BLOOM\n\t\t\tresult = applyBloom(result, uv0);\n\t\t#endif\n\t\t#ifdef COLOR_ENHANCE\n\t\t\tresult = applyColorEnhance(result);\n\t\t#endif\n\t\t#ifdef GRADING\n\t\t\tresult = applyGrading(result);\n\t\t#endif\n\t\tresult = toneMap(max(vec3f(0.0), result));\n\t\t#ifdef COLOR_LUT\n\t\t\tresult = applyColorLUT(result);\n\t\t#endif\n\t\t#ifdef VIGNETTE\n\t\t\tresult = applyVignette(result, uv);\n\t\t#endif\n\t\t#include "composeMainEndPS"\n\t\t#ifdef DEBUG_COMPOSE\n\t\t\t#if DEBUG_COMPOSE == scene\n\t\t\t\tresult = scene.rgb;\n\t\t\t#elif defined(BLOOM) && DEBUG_COMPOSE == bloom\n\t\t\t\tresult = dBloom * uniform.bloomIntensity;\n\t\t\t#elif defined(DOF) && DEBUG_COMPOSE == dofcoc\n\t\t\t\tresult = vec3f(dCoc, 0.0);\n\t\t\t#elif defined(DOF) && DEBUG_COMPOSE == dofblur\n\t\t\t\tresult = dBlur;\n\t\t\t#elif defined(SSAO_TEXTURE) && DEBUG_COMPOSE == ssao\n\t\t\t\tresult = vec3f(dSsao);\n\t\t\t#elif defined(VIGNETTE) && DEBUG_COMPOSE == vignette\n\t\t\t\tresult = vec3f(dVignette);\n\t\t\t#endif\n\t\t#endif\n\t\tresult = gammaCorrectOutput(result);\n\t\toutput.color = vec4f(result, scene.a);\n\t\treturn output;\n\t}\n',composeBloomPS:"\n\t#ifdef BLOOM\n\t\tvar bloomTexture: texture_2d<f32>;\n\t\tvar bloomTextureSampler: sampler;\n\t\tuniform bloomIntensity: f32;\n\t\t\n\t\tvar<private> dBloom: vec3f;\n\t\t\n\t\tfn applyBloom(color: vec3f, uv: vec2f) -> vec3f {\n\t\t\tdBloom = textureSampleLevel(bloomTexture, bloomTextureSampler, uv, 0.0).rgb;\n\t\t\treturn color + dBloom * uniform.bloomIntensity;\n\t\t}\n\t#endif\n",composeDofPS:"\n\t#ifdef DOF\n\t\tvar cocTexture: texture_2d<f32>;\n\t\tvar cocTextureSampler: sampler;\n\t\tvar blurTexture: texture_2d<f32>;\n\t\tvar blurTextureSampler: sampler;\n\t\t\n\t\tvar<private> dCoc: vec2f;\n\t\tvar<private> dBlur: vec3f;\n\t\tfn getDofBlur(uv: vec2f) -> vec3f {\n\t\t\tdCoc = textureSampleLevel(cocTexture, cocTextureSampler, uv, 0.0).rg;\n\t\t\t#if DOF_UPSCALE\n\t\t\t\tlet blurTexelSize = 1.0 / vec2f(textureDimensions(blurTexture, 0));\n\t\t\t\tvar bilinearBlur = vec3f(0.0);\n\t\t\t\tvar totalWeight = 0.0;\n\t\t\t\tfor (var i = -1; i <= 1; i++) {\n\t\t\t\t\tfor (var j = -1; j <= 1; j++) {\n\t\t\t\t\t\tlet offset = vec2f(f32(i), f32(j)) * blurTexelSize;\n\t\t\t\t\t\tlet cocSample = textureSampleLevel(cocTexture, cocTextureSampler, uv + offset, 0.0).rg;\n\t\t\t\t\t\tlet blurSample = textureSampleLevel(blurTexture, blurTextureSampler, uv + offset, 0.0).rgb;\n\t\t\t\t\t\tlet cocWeight = clamp(cocSample.r + cocSample.g, 0.0, 1.0);\n\t\t\t\t\t\tbilinearBlur += blurSample * cocWeight;\n\t\t\t\t\t\ttotalWeight += cocWeight;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t\tif (totalWeight > 0.0) {\n\t\t\t\t\tbilinearBlur /= totalWeight;\n\t\t\t\t}\n\t\t\t\tdBlur = bilinearBlur;\n\t\t\t\treturn bilinearBlur;\n\t\t\t#else\n\t\t\t\tdBlur = textureSampleLevel(blurTexture, blurTextureSampler, uv, 0.0).rgb;\n\t\t\t\treturn dBlur;\n\t\t\t#endif\n\t\t}\n\t\tfn applyDof(color: vec3f, uv: vec2f) -> vec3f {\n\t\t\tlet blur = getDofBlur(uv);\n\t\t\treturn mix(color, blur, dCoc.r + dCoc.g);\n\t\t}\n\t#endif\n",composeSsaoPS:"\n\t#ifdef SSAO\n\t\t#define SSAO_TEXTURE\n\t#endif\n\t#if DEBUG_COMPOSE == ssao\n\t\t#define SSAO_TEXTURE\n\t#endif\n\t#ifdef SSAO_TEXTURE\n\t\tvar ssaoTexture: texture_2d<f32>;\n\t\tvar ssaoTextureSampler: sampler;\n\t\t\n\t\tvar<private> dSsao: f32;\n\t\t\n\t\tfn applySsao(color: vec3f, uv: vec2f) -> vec3f {\n\t\t\tdSsao = textureSampleLevel(ssaoTexture, ssaoTextureSampler, uv, 0.0).r;\n\t\t\t\n\t\t\t#ifdef SSAO\n\t\t\t\treturn color * dSsao;\n\t\t\t#else\n\t\t\t\treturn color;\n\t\t\t#endif\n\t\t}\n\t#endif\n",composeGradingPS:"\n\t#ifdef GRADING\n\t\tuniform brightnessContrastSaturation: vec3f;\n\t\tuniform tint: vec3f;\n\t\tfn colorGradingHDR(color: vec3f, brt: f32, sat: f32, con: f32) -> vec3f {\n\t\t\tvar colorOut = color * uniform.tint;\n\t\t\tcolorOut = colorOut * brt;\n\t\t\tlet grey = dot(colorOut, vec3f(0.3, 0.59, 0.11));\n\t\t\tlet normalizedGrey = grey / max(1.0, max(colorOut.r, max(colorOut.g, colorOut.b)));\n\t\t\tcolorOut = mix(vec3f(normalizedGrey), colorOut, sat);\n\t\t\treturn mix(vec3f(0.5), colorOut, con);\n\t\t}\n\t\tfn applyGrading(color: vec3f) -> vec3f {\n\t\t\treturn colorGradingHDR(color, \n\t\t\t\tuniform.brightnessContrastSaturation.x, \n\t\t\t\tuniform.brightnessContrastSaturation.z, \n\t\t\t\tuniform.brightnessContrastSaturation.y);\n\t\t}\n\t#endif\n",composeColorEnhancePS:"\n\t#ifdef COLOR_ENHANCE\n\t\tuniform colorEnhanceParams: vec4f;\n\t\tuniform colorEnhanceMidtones: f32;\n\t\tfn applyColorEnhance(color: vec3f) -> vec3f {\n\t\t\tvar colorOut = color;\n\t\t\tvar maxChannel = max(colorOut.r, max(colorOut.g, colorOut.b));\n\t\t\tvar lum = dot(colorOut, vec3f(0.2126, 0.7152, 0.0722));\n\t\t\tif (uniform.colorEnhanceParams.x != 0.0 || uniform.colorEnhanceParams.y != 0.0) {\n\t\t\t\tvar logLum = log2(max(lum, 0.001)) / 10.0 + 0.5;\n\t\t\t\tlogLum = clamp(logLum, 0.0, 1.0);\n\t\t\t\tlet shadowWeight = pow(1.0 - logLum, 2.0);\n\t\t\t\tlet highlightWeight = pow(logLum, 2.0);\n\t\t\t\tcolorOut *= pow(2.0, uniform.colorEnhanceParams.x * shadowWeight);\n\t\t\t\tcolorOut *= pow(2.0, uniform.colorEnhanceParams.y * highlightWeight);\n\t\t\t}\n\t\t\tif (uniform.colorEnhanceMidtones != 0.0) {\n\t\t\t\tlet pivot = 0.18;\n\t\t\t\tlet widthStops = 1.25;\n\t\t\t\tlet maxStops = 2.0;\n\t\t\t\tlet y = max(dot(colorOut, vec3f(0.2126, 0.7152, 0.0722)), 1e-6);\n\t\t\t\tlet d = log2(y / pivot);\n\t\t\t\tlet w = exp(-(d * d) / (2.0 * widthStops * widthStops));\n\t\t\t\tlet stops = uniform.colorEnhanceMidtones * maxStops * w;\n\t\t\t\tcolorOut *= exp2(stops);\n\t\t\t}\n\t\t\tif (uniform.colorEnhanceParams.z != 0.0) {\n\t\t\t\tlet minChannel = min(colorOut.r, min(colorOut.g, colorOut.b));\n\t\t\t\tmaxChannel = max(colorOut.r, max(colorOut.g, colorOut.b));\n\t\t\t\tlet sat = (maxChannel - minChannel) / max(maxChannel, 0.001);\n\t\t\t\tlum = dot(colorOut, vec3f(0.2126, 0.7152, 0.0722));\n\t\t\t\tlet normalizedLum = lum / max(1.0, maxChannel);\n\t\t\t\tlet grey = vec3f(normalizedLum) * maxChannel;\n\t\t\t\tlet satBoost = uniform.colorEnhanceParams.z * (1.0 - sat);\n\t\t\t\tcolorOut = mix(grey, colorOut, 1.0 + satBoost);\n\t\t\t}\n\t\t\tif (uniform.colorEnhanceParams.w != 0.0) {\n\t\t\t\tmaxChannel = max(colorOut.r, max(colorOut.g, colorOut.b));\n\t\t\t\tlet scale = max(1.0, maxChannel);\n\t\t\t\tlet normalized = colorOut / scale;\n\t\t\t\tlet darkChannel = min(normalized.r, min(normalized.g, normalized.b));\n\t\t\t\tlet atmosphericLight = 0.95;\n\t\t\t\tvar t = 1.0 - uniform.colorEnhanceParams.w * darkChannel / atmosphericLight;\n\t\t\t\tt = max(t, 0.1);\n\t\t\t\tlet dehazed = (normalized - atmosphericLight) / t + atmosphericLight;\n\t\t\t\tcolorOut = dehazed * scale;\n\t\t\t}\n\t\t\treturn max(vec3f(0.0), colorOut);\n\t\t}\n\t#endif\n",composeVignettePS:"\n\t#ifdef VIGNETTE\n\t\tuniform vignetterParams: vec4f;\n\t\tuniform vignetteColor: vec3f;\n\t\t\n\t\tvar<private> dVignette: f32;\n\t\t\n\t\tfn calcVignette(uv: vec2f) -> f32 {\n\t\t\tlet inner = uniform.vignetterParams.x;\n\t\t\tlet outer = uniform.vignetterParams.y;\n\t\t\tlet curvature = uniform.vignetterParams.z;\n\t\t\tlet intensity = uniform.vignetterParams.w;\n\t\t\tlet curve = pow(abs(uv * 2.0 - 1.0), vec2f(1.0 / curvature));\n\t\t\tlet edge = pow(length(curve), curvature);\n\t\t\tdVignette = 1.0 - intensity * smoothstep(inner, outer, edge);\n\t\t\treturn dVignette;\n\t\t}\n\t\tfn applyVignette(color: vec3f, uv: vec2f) -> vec3f {\n\t\t\treturn mix(uniform.vignetteColor, color, calcVignette(uv));\n\t\t}\n\t#endif\n",composeFringingPS:"\n\t#ifdef FRINGING\n\t\tuniform fringingIntensity: f32;\n\t\tfn applyFringing(color: vec3f, uv: vec2f) -> vec3f {\n\t\t\tlet centerDistance = uv - 0.5;\n\t\t\tlet offset = uniform.fringingIntensity * centerDistance * centerDistance;\n\t\t\tvar colorOut = color;\n\t\t\tcolorOut.r = textureSample(sceneTexture, sceneTextureSampler, uv - offset).r;\n\t\t\tcolorOut.b = textureSample(sceneTexture, sceneTextureSampler, uv + offset).b;\n\t\t\treturn colorOut;\n\t\t}\n\t#endif\n",composeCasPS:"\n\t#ifdef CAS\n\t\tuniform sharpness: f32;\n\t\t#ifdef CAS_HDR\n\t\t\tfn maxComponent(x: f32, y: f32, z: f32) -> f32 { return max(x, max(y, z)); }\n\t\t\tfn toSDR(c: vec3f) -> vec3f { return c / (1.0 + maxComponent(c.r, c.g, c.b)); }\n\t\t\tfn toHDR(c: vec3f) -> vec3f { return c / max(1.0 - maxComponent(c.r, c.g, c.b), 1e-4); }\n\t\t#else\n\t\t\tfn toSDR(c: vec3f) -> vec3f { return c; }\n\t\t\tfn toHDR(c: vec3f) -> vec3f { return c; }\n\t\t#endif\n\t\tfn applyCas(color: vec3f, uv: vec2f, sharpness: f32) -> vec3f {\n\t\t\tlet x = uniform.sceneTextureInvRes.x;\n\t\t\tlet y = uniform.sceneTextureInvRes.y;\n\t\t\tlet a: half3 = half3(toSDR(textureSampleLevel(sceneTexture, sceneTextureSampler, uv + vec2f(0.0, -y), 0.0).rgb));\n\t\t\tlet b: half3 = half3(toSDR(textureSampleLevel(sceneTexture, sceneTextureSampler, uv + vec2f(-x, 0.0), 0.0).rgb));\n\t\t\tlet c: half3 = half3(toSDR(color.rgb));\n\t\t\tlet d: half3 = half3(toSDR(textureSampleLevel(sceneTexture, sceneTextureSampler, uv + vec2f(x, 0.0), 0.0).rgb));\n\t\t\tlet e: half3 = half3(toSDR(textureSampleLevel(sceneTexture, sceneTextureSampler, uv + vec2f(0.0, y), 0.0).rgb));\n\t\t\tlet min_g = min(a.g, min(b.g, min(c.g, min(d.g, e.g))));\n\t\t\tlet max_g = max(a.g, max(b.g, max(c.g, max(d.g, e.g))));\n\t\t\tlet sharpening_amount = sqrt(min(half(1.0) - max_g, min_g) / max(max_g, half(1e-4)));\n\t\t\tlet w = sharpening_amount * half(sharpness);\n\t\t\tvar res = (w * (a + b + d + e) + c) / (half(4.0) * w + half(1.0));\n\t\t\tres = max(res, half3(0.0));\n\t\t\treturn toHDR(vec3f(res));\n\t\t}\n\t#endif\n",composeColorLutPS:"\n\t#ifdef COLOR_LUT\n\t\tvar colorLUT: texture_2d<f32>;\n\t\tvar colorLUTSampler: sampler;\n\t\tuniform colorLUTParams: vec4f;\n\t\tfn applyColorLUT(color: vec3f) -> vec3f {\n\t\t\tvar c: vec3f = clamp(color, vec3f(0.0), vec3f(1.0));\n\t\t\tlet width: f32 = uniform.colorLUTParams.x;\n\t\t\tlet height: f32 = uniform.colorLUTParams.y;\n\t\t\tlet maxColor: f32 = uniform.colorLUTParams.z;\n\t\t\tlet cell: f32 = c.b * maxColor;\n\t\t\tlet cell_l: f32 = floor(cell);\n\t\t\tlet cell_h: f32 = ceil(cell);\n\t\t\tlet half_px_x: f32 = 0.5 / width;\n\t\t\tlet half_px_y: f32 = 0.5 / height;\n\t\t\tlet r_offset: f32 = half_px_x + c.r / height * (maxColor / height);\n\t\t\tlet g_offset: f32 = half_px_y + c.g * (maxColor / height);\n\t\t\tlet uv_l: vec2f = vec2f(cell_l / height + r_offset, g_offset);\n\t\t\tlet uv_h: vec2f = vec2f(cell_h / height + r_offset, g_offset);\n\t\t\tlet color_l: vec3f = textureSampleLevel(colorLUT, colorLUTSampler, uv_l, 0.0).rgb;\n\t\t\tlet color_h: vec3f = textureSampleLevel(colorLUT, colorLUTSampler, uv_h, 0.0).rgb;\n\t\t\tlet lutColor: vec3f = mix(color_l, color_h, fract(cell));\n\t\t\treturn mix(color, lutColor, uniform.colorLUTParams.w);\n\t\t}\n\t#endif\n",composeDeclarationsPS:"",composeMainStartPS:"",composeMainEndPS:""};class C extends p.s{set debug(t){this._debug!==t&&(this._debug=t,this._shaderDirty=!0)}get debug(){return this._debug}set colorLUT(t){this._colorLUT!==t&&(this._colorLUT=t,this._shaderDirty=!0)}get colorLUT(){return this._colorLUT}set bloomTexture(t){this._bloomTexture!==t&&(this._bloomTexture=t,this._shaderDirty=!0)}get bloomTexture(){return this._bloomTexture}set cocTexture(t){this._cocTexture!==t&&(this._cocTexture=t,this._shaderDirty=!0)}get cocTexture(){return this._cocTexture}set ssaoTexture(t){this._ssaoTexture!==t&&(this._ssaoTexture=t,this._shaderDirty=!0)}get ssaoTexture(){return this._ssaoTexture}set taaEnabled(t){this._taaEnabled!==t&&(this._taaEnabled=t,this._shaderDirty=!0)}get taaEnabled(){return this._taaEnabled}set gradingEnabled(t){this._gradingEnabled!==t&&(this._gradingEnabled=t,this._shaderDirty=!0)}get gradingEnabled(){return this._gradingEnabled}set vignetteEnabled(t){this._vignetteEnabled!==t&&(this._vignetteEnabled=t,this._shaderDirty=!0)}get vignetteEnabled(){return this._vignetteEnabled}set fringingEnabled(t){this._fringingEnabled!==t&&(this._fringingEnabled=t,this._shaderDirty=!0)}get fringingEnabled(){return this._fringingEnabled}set colorEnhanceEnabled(t){this._colorEnhanceEnabled!==t&&(this._colorEnhanceEnabled=t,this._shaderDirty=!0)}get colorEnhanceEnabled(){return this._colorEnhanceEnabled}set toneMapping(t){this._toneMapping!==t&&(this._toneMapping=t,this._shaderDirty=!0)}get toneMapping(){return this._toneMapping}set sharpness(t){this._sharpness!==t&&(this._sharpness=t,this._shaderDirty=!0)}get sharpness(){return this._sharpness}get isSharpnessEnabled(){return this._sharpness>0}set hdrScene(t){this._hdrScene!==t&&(this._hdrScene=t,this._shaderDirty=!0)}get hdrScene(){return this._hdrScene}postInit(){this.setClearColor(r.Q.BLACK),this.setClearDepth(1),this.setClearStencil(0)}frameUpdate(){const t=(this.renderTarget??this.device.backBuffer).isColorBufferSrgb(0)?l.hcE:l.JdY;this._gammaCorrection!==t&&(this._gammaCorrection=t,this._shaderDirty=!0);const e=x.W.get(this.device,this.device.isWebGPU?i.g80:i.BzK);for(const[n,r]of this._customComposeChunks.entries()){const t=e.get(n);t!==r&&(this._customComposeChunks.set(n,t),this._shaderDirty=!0)}if(this._shaderDirty){this._shaderDirty=!1;const t=l.IaO[this._gammaCorrection],n=this._customComposeChunks,r=(0,T.s)(n.get("composeDeclarationsPS")??""),s=(0,T.s)(n.get("composeMainStartPS")??""),o=(0,T.s)(n.get("composeMainEndPS")??""),a=`${this.toneMapping}-${t}-`+(this.bloomTexture?"bloom":"nobloom")+"-"+(this.cocTexture?"dof":"nodof")+"-"+(this.blurTextureUpscale?"dofupscale":"")+"-"+(this.ssaoTexture?"ssao":"nossao")+"-"+(this.gradingEnabled?"grading":"nograding")+"-"+(this.colorEnhanceEnabled?"colorenhance":"nocolorenhance")+"-"+(this.colorLUT?"colorlut":"nocolorlut")+"-"+(this.vignetteEnabled?"vignette":"novignette")+"-"+(this.fringingEnabled?"fringing":"nofringing")+"-"+(this.taaEnabled?"taa":"notaa")+"-"+(this.isSharpnessEnabled?this._hdrScene?"cashdr":"cas":"nocas")+`-${this._debug??""}`+`-decl${r}-start${s}-end${o}`;if(this._key!==a){this._key=a;const n=new Map;n.set("TONEMAP",l.lh8[this.toneMapping]),n.set("GAMMA",t),this.bloomTexture&&n.set("BLOOM",!0),this.cocTexture&&n.set("DOF",!0),this.blurTextureUpscale&&n.set("DOF_UPSCALE",!0),this.ssaoTexture&&n.set("SSAO",!0),this.gradingEnabled&&n.set("GRADING",!0),this.colorEnhanceEnabled&&n.set("COLOR_ENHANCE",!0),this.colorLUT&&n.set("COLOR_LUT",!0),this.vignetteEnabled&&n.set("VIGNETTE",!0),this.fringingEnabled&&n.set("FRINGING",!0),this.taaEnabled&&n.set("TAA",!0),this.isSharpnessEnabled&&(n.set("CAS",!0),this._hdrScene&&n.set("CAS_HDR",!0)),this._debug&&n.set("DEBUG_COMPOSE",this._debug);const r=new Map(e);this.shader=m.lo.createShader(this.device,{uniqueName:`ComposeShader-${a}`,attributes:{aPosition:i.JYe},vertexChunk:"quadVS",fragmentChunk:"composePS",fragmentDefines:n,fragmentIncludes:r})}}}execute(){const t=this.sceneTexture;this.sceneTextureId.setValue(t),this.sceneTextureInvResValue[0]=1/t.width,this.sceneTextureInvResValue[1]=1/t.height,this.sceneTextureInvResId.setValue(this.sceneTextureInvResValue),this._bloomTexture&&(this.bloomTextureId.setValue(this._bloomTexture),this.bloomIntensityId.setValue(this.bloomIntensity)),this._cocTexture&&(this.cocTextureId.setValue(this._cocTexture),this.blurTextureId.setValue(this.blurTexture)),this._ssaoTexture&&this.ssaoTextureId.setValue(this._ssaoTexture),this._gradingEnabled&&(this.bcsId.setValue([this.gradingBrightness,this.gradingContrast,this.gradingSaturation]),this.tintId.setValue([this.gradingTint.r,this.gradingTint.g,this.gradingTint.b])),this._colorEnhanceEnabled&&(this.colorEnhanceParamsId.setValue([this.colorEnhanceShadows,this.colorEnhanceHighlights,this.colorEnhanceVibrance,this.colorEnhanceDehaze]),this.colorEnhanceMidtonesId.setValue(this.colorEnhanceMidtones));const e=this._colorLUT;e&&(this.colorLUTParams[0]=e.width,this.colorLUTParams[1]=e.height,this.colorLUTParams[2]=e.height-1,this.colorLUTParams[3]=this.colorLUTIntensity,this.colorLUTParamsId.setValue(this.colorLUTParams),this.colorLUTId.setValue(e)),this._vignetteEnabled&&(this.vignetterParamsId.setValue([this.vignetteInner,this.vignetteOuter,this.vignetteCurvature,this.vignetteIntensity]),this.vignetteColorId.setValue([this.vignetteColor.r,this.vignetteColor.g,this.vignetteColor.b])),this._fringingEnabled&&this.fringingIntensityId.setValue(this.fringingIntensity/1024),this.isSharpnessEnabled&&this.sharpnessId.setValue(o.D.lerp(-.125,-.2,this.sharpness)),super.execute()}constructor(t){super(t),this.sceneTexture=null,this.bloomIntensity=.01,this._bloomTexture=null,this._cocTexture=null,this.blurTexture=null,this.blurTextureUpscale=!1,this._ssaoTexture=null,this._toneMapping=l.llM,this._gradingEnabled=!1,this.gradingSaturation=1,this.gradingContrast=1,this.gradingBrightness=1,this.gradingTint=new r.Q(1,1,1,1),this._shaderDirty=!0,this._vignetteEnabled=!1,this.vignetteInner=.5,this.vignetteOuter=1,this.vignetteCurvature=.5,this.vignetteIntensity=.3,this.vignetteColor=new r.Q(0,0,0),this._fringingEnabled=!1,this.fringingIntensity=10,this._colorEnhanceEnabled=!1,this.colorEnhanceShadows=0,this.colorEnhanceHighlights=0,this.colorEnhanceVibrance=0,this.colorEnhanceDehaze=0,this.colorEnhanceMidtones=0,this._taaEnabled=!1,this._hdrScene=!0,this._sharpness=.5,this._gammaCorrection=l.JdY,this._colorLUT=null,this.colorLUTIntensity=1,this._key="",this._debug=null,this._customComposeChunks=new Map([["composeDeclarationsPS",""],["composeMainStartPS",""],["composeMainEndPS",""]]),x.W.get(t,i.BzK).add(S,!1),x.W.get(t,i.g80).add(P,!1);const{scope:e}=t;this.sceneTextureId=e.resolve("sceneTexture"),this.bloomTextureId=e.resolve("bloomTexture"),this.cocTextureId=e.resolve("cocTexture"),this.ssaoTextureId=e.resolve("ssaoTexture"),this.blurTextureId=e.resolve("blurTexture"),this.bloomIntensityId=e.resolve("bloomIntensity"),this.bcsId=e.resolve("brightnessContrastSaturation"),this.tintId=e.resolve("tint"),this.vignetterParamsId=e.resolve("vignetterParams"),this.vignetteColorId=e.resolve("vignetteColor"),this.fringingIntensityId=e.resolve("fringingIntensity"),this.sceneTextureInvResId=e.resolve("sceneTextureInvRes"),this.sceneTextureInvResValue=new Float32Array(2),this.sharpnessId=e.resolve("sharpness"),this.colorLUTId=e.resolve("colorLUT"),this.colorLUTParams=new Float32Array(4),this.colorLUTParamsId=e.resolve("colorLUTParams"),this.colorEnhanceParamsId=e.resolve("colorEnhanceParams"),this.colorEnhanceMidtonesId=e.resolve("colorEnhanceMidtones")}}class _ extends p.s{destroy(){this.renderTarget&&(this.renderTarget.destroyTextureBuffers(),this.renderTarget.destroy(),this.renderTarget=null)}setup(){for(let t=0;t<2;++t)this.historyTextures[t]=new u.g(this.device,{name:`TAA-History-${t}`,width:4,height:4,format:this.sourceTexture.format,mipmaps:!1,minFilter:i.DrA,magFilter:i.DrA,addressU:i.vq6,addressV:i.vq6}),this.historyRenderTargets[t]=new d.O({colorBuffer:this.historyTextures[t],depth:!1});this.historyTexture=this.historyTextures[0],this.init(this.historyRenderTargets[0],{resizeSource:this.sourceTexture})}before(){this.sourceTextureId.setValue(this.sourceTexture),this.historyTextureId.setValue(this.historyTextures[1-this.historyIndex]),this.textureSize[0]=this.sourceTexture.width,this.textureSize[1]=this.sourceTexture.height,this.textureSizeId.setValue(this.textureSize);const t=this.cameraComponent.camera;this.viewProjPrevId.setValue(t._viewProjPrevious.data),this.viewProjInvId.setValue(t._viewProjInverse.data),this.jittersId.setValue(t._jitters),this.cameraParamsId.setValue(t.fillShaderParams(this.cameraParams))}update(){return this.historyIndex=1-this.historyIndex,this.historyTexture=this.historyTextures[this.historyIndex],this.renderTarget=this.historyRenderTargets[this.historyIndex],this.historyTexture}constructor(t,e,n){super(t),this.historyIndex=0,this.historyTexture=null,this.historyTextures=[],this.historyRenderTargets=[],this.sourceTexture=e,this.cameraComponent=n,x.W.get(t,i.BzK).set("sampleCatmullRomPS","\nvec4 SampleTextureCatmullRom(TEXTURE_ACCEPT(tex), vec2 uv, vec2 texSize) {\n\tvec2 samplePos = uv * texSize;\n\tvec2 texPos1 = floor(samplePos - 0.5) + 0.5;\n\tvec2 f = samplePos - texPos1;\n\tvec2 w0 = f * (-0.5 + f * (1.0 - 0.5 * f));\n\tvec2 w1 = 1.0 + f * f * (-2.5 + 1.5 * f);\n\tvec2 w2 = f * (0.5 + f * (2.0 - 1.5 * f));\n\tvec2 w3 = f * f * (-0.5 + 0.5 * f);\n\tvec2 w12 = w1 + w2;\n\tvec2 offset12 = w2 / (w1 + w2);\n\tvec2 texPos0 = (texPos1 - 1.0) / texSize;\n\tvec2 texPos3 = (texPos1 + 2.0) / texSize;\n\tvec2 texPos12 = (texPos1 + offset12) / texSize;\n\tvec4 result = vec4(0.0);\n\tresult += texture2DLod(tex, vec2(texPos0.x, texPos0.y), 0.0) * w0.x * w0.y;\n\tresult += texture2DLod(tex, vec2(texPos12.x, texPos0.y), 0.0) * w12.x * w0.y;\n\tresult += texture2DLod(tex, vec2(texPos3.x, texPos0.y), 0.0) * w3.x * w0.y;\n\tresult += texture2DLod(tex, vec2(texPos0.x, texPos12.y), 0.0) * w0.x * w12.y;\n\tresult += texture2DLod(tex, vec2(texPos12.x, texPos12.y), 0.0) * w12.x * w12.y;\n\tresult += texture2DLod(tex, vec2(texPos3.x, texPos12.y), 0.0) * w3.x * w12.y;\n\tresult += texture2DLod(tex, vec2(texPos0.x, texPos3.y), 0.0) * w0.x * w3.y;\n\tresult += texture2DLod(tex, vec2(texPos12.x, texPos3.y), 0.0) * w12.x * w3.y;\n\tresult += texture2DLod(tex, vec2(texPos3.x, texPos3.y), 0.0) * w3.x * w3.y;\n\treturn result;\n}\n"),x.W.get(t,i.g80).set("sampleCatmullRomPS","\nfn SampleTextureCatmullRom(tex: texture_2d<f32>, texSampler: sampler, uv: vec2f, texSize: vec2f) -> vec4f {\n\tlet samplePos: vec2f = uv * texSize;\n\tlet texPos1: vec2f = floor(samplePos - 0.5) + 0.5;\n\tlet f: vec2f = samplePos - texPos1;\n\tlet w0: vec2f = f * (-0.5 + f * (1.0 - 0.5 * f));\n\tlet w1: vec2f = 1.0 + f * f * (-2.5 + 1.5 * f);\n\tlet w2: vec2f = f * (0.5 + f * (2.0 - 1.5 * f));\n\tlet w3: vec2f = f * f * (-0.5 + 0.5 * f);\n\tlet w12: vec2f = w1 + w2;\n\tlet offset12: vec2f = w2 / w12;\n\tlet texPos0: vec2f = (texPos1 - 1.0) / texSize;\n\tlet texPos3: vec2f = (texPos1 + 2.0) / texSize;\n\tlet texPos12: vec2f = (texPos1 + offset12) / texSize;\n\tvar result: vec4f = vec4f(0.0);\n\tresult = result + textureSampleLevel(tex, texSampler, vec2f(texPos0.x, texPos0.y), 0.0) * w0.x * w0.y;\n\tresult = result + textureSampleLevel(tex, texSampler, vec2f(texPos12.x, texPos0.y), 0.0) * w12.x * w0.y;\n\tresult = result + textureSampleLevel(tex, texSampler, vec2f(texPos3.x, texPos0.y), 0.0) * w3.x * w0.y;\n\tresult = result + textureSampleLevel(tex, texSampler, vec2f(texPos0.x, texPos12.y), 0.0) * w0.x * w12.y;\n\tresult = result + textureSampleLevel(tex, texSampler, vec2f(texPos12.x, texPos12.y), 0.0) * w12.x * w12.y;\n\tresult = result + textureSampleLevel(tex, texSampler, vec2f(texPos3.x, texPos12.y), 0.0) * w3.x * w12.y;\n\tresult = result + textureSampleLevel(tex, texSampler, vec2f(texPos0.x, texPos3.y), 0.0) * w0.x * w3.y;\n\tresult = result + textureSampleLevel(tex, texSampler, vec2f(texPos12.x, texPos3.y), 0.0) * w12.x * w3.y;\n\tresult = result + textureSampleLevel(tex, texSampler, vec2f(texPos3.x, texPos3.y), 0.0) * w3.x * w3.y;\n\treturn result;\n}\n"),x.W.get(t,i.BzK).set("taaResolvePS",'\n\t#include  "sampleCatmullRomPS"\n\t#include  "screenDepthPS"\n\tuniform sampler2D sourceTexture;\n\tuniform sampler2D historyTexture;\n\tuniform mat4 matrix_viewProjectionPrevious;\n\tuniform mat4 matrix_viewProjectionInverse;\n\tuniform vec4 jitters;\n\tuniform vec2 textureSize;\n\tvarying vec2 uv0;\n\tvec2 reproject(vec2 uv, float depth) {\n\t\tdepth = depth * 2.0 - 1.0;\n\t\tvec4 ndc = vec4(uv * 2.0 - 1.0, depth, 1.0);\n\t\tndc.xy -= jitters.xy;\n\t\tvec4 worldPosition = matrix_viewProjectionInverse * ndc;\n\t\tworldPosition /= worldPosition.w;\n\t\tvec4 screenPrevious = matrix_viewProjectionPrevious * worldPosition;\n\t\treturn (screenPrevious.xy / screenPrevious.w) * 0.5 + 0.5;\n\t}\n\tvec4 colorClamp(vec2 uv, vec4 historyColor) {\n\t\tvec3 minColor = vec3(9999.0);\n\t\tvec3 maxColor = vec3(-9999.0);\n\t\tfor(float x = -1.0; x <= 1.0; ++x) {\n\t\t\tfor(float y = -1.0; y <= 1.0; ++y) {\n\t\t\t\tvec3 color = texture2D(sourceTexture, uv + vec2(x, y) / textureSize).rgb;\n\t\t\t\tminColor = min(minColor, color);\n\t\t\t\tmaxColor = max(maxColor, color);\n\t\t\t}\n\t\t}\n\t\tvec3 clamped = clamp(historyColor.rgb, minColor, maxColor);\n\t\treturn vec4(clamped, historyColor.a);\n\t}\n\tvoid main()\n\t{\n\t\tvec4 srcColor = texture2D(sourceTexture, uv0);\n\t\tfloat linearDepth = getLinearScreenDepth(uv0);\n\t\tfloat depth = delinearizeDepth(linearDepth);\n\t\tvec2 historyUv = reproject(uv0, depth);\n\t\t#ifdef QUALITY_HIGH\n\t\t\tvec4 historyColor = SampleTextureCatmullRom(TEXTURE_PASS(historyTexture), historyUv, textureSize);\n\t\t#else\n\t\t\tvec4 historyColor = texture2D(historyTexture, historyUv);\n\t\t#endif\n\t\tvec4 historyColorClamped = colorClamp(uv0, historyColor);\n\t\tfloat mixFactor = (historyUv.x < 0.0 || historyUv.x > 1.0 || historyUv.y < 0.0 || historyUv.y > 1.0) ?\n\t\t\t1.0 : 0.05;\n\t\tgl_FragColor = mix(historyColorClamped, srcColor, mixFactor);\n\t}\n'),x.W.get(t,i.g80).set("taaResolvePS",'\n\t#include "sampleCatmullRomPS"\n\t#include "screenDepthPS"\n\tvar sourceTexture: texture_2d<f32>;\n\tvar sourceTextureSampler: sampler;\n\tvar historyTexture: texture_2d<f32>;\n\tvar historyTextureSampler: sampler;\n\tuniform matrix_viewProjectionPrevious: mat4x4f;\n\tuniform matrix_viewProjectionInverse: mat4x4f;\n\tuniform jitters: vec4f;\n\tuniform textureSize: vec2f;\n\tvarying uv0: vec2f;\n\tfn reproject(uv_in: vec2f, depth: f32) -> vec2f {\n\t\tvar uv = vec2f(uv_in.x, 1.0 - uv_in.y);\n\t\tvar ndc = vec4f(uv * 2.0 - 1.0, depth, 1.0);\n\t\tndc = vec4f(ndc.xy - uniform.jitters.xy, ndc.zw);\n\t\tvar worldPosition = uniform.matrix_viewProjectionInverse * ndc;\n\t\tworldPosition = worldPosition / worldPosition.w;\n\t\tlet screenPrevious = uniform.matrix_viewProjectionPrevious * worldPosition;\n\t\tvar result = (screenPrevious.xy / screenPrevious.w) * 0.5 + 0.5;\n\t\tresult.y = 1.0 - result.y;\n\t\treturn result;\n\t}\n\tfn colorClamp(uv: vec2f, historyColor: vec4f) -> vec4f {\n\t\tvar minColor = vec3f(9999.0);\n\t\tvar maxColor = vec3f(-9999.0);\n\t\tfor (var ix: i32 = -1; ix <= 1; ix = ix + 1) {\n\t\t\tfor (var iy: i32 = -1; iy <= 1; iy = iy + 1) {\n\t\t\t\tlet color_sample = textureSample(sourceTexture, sourceTextureSampler, uv + vec2f(f32(ix), f32(iy)) / uniform.textureSize).rgb;\n\t\t\t\tminColor = min(minColor, color_sample);\n\t\t\t\tmaxColor = max(maxColor, color_sample);\n\t\t\t}\n\t\t}\n\t\tlet clamped = clamp(historyColor.rgb, minColor, maxColor);\n\t\treturn vec4f(clamped, historyColor.a);\n\t}\n\t@fragment\n\tfn fragmentMain(input: FragmentInput) -> FragmentOutput {\n\t\tvar output: FragmentOutput;\n\t\tlet srcColor = textureSample(sourceTexture, sourceTextureSampler, uv0);\n\t\tlet linearDepth = getLinearScreenDepth(uv0);\n\t\tlet depth = delinearizeDepth(linearDepth);\n\t\tlet historyUv = reproject(uv0, depth);\n\t\t#ifdef QUALITY_HIGH\n\t\t\tvar historyColor: vec4f = SampleTextureCatmullRom(historyTexture, historyTextureSampler, historyUv, uniform.textureSize);\n\t\t#else\n\t\t\tvar historyColor: vec4f = textureSample(historyTexture, historyTextureSampler, historyUv);\n\t\t#endif\n\t\tlet historyColorClamped = colorClamp(uv0, historyColor);\n\t\tlet mixFactor_condition = historyUv.x < 0.0 || historyUv.x > 1.0 || historyUv.y < 0.0 || historyUv.y > 1.0;\n\t\tlet mixFactor = select(0.05, 1.0, mixFactor_condition);\n\t\toutput.color = mix(historyColorClamped, srcColor, mixFactor);\n\t\treturn output;\n\t}\n');const r=new Map;r.set("QUALITY_HIGH",!0),m.lo.addScreenDepthChunkDefines(t,n.shaderParams,r),this.shader=m.lo.createShader(t,{uniqueName:"TaaResolveShader",attributes:{aPosition:i.JYe},vertexChunk:"quadVS",fragmentChunk:"taaResolvePS",fragmentDefines:r});const{scope:s}=t;this.sourceTextureId=s.resolve("sourceTexture"),this.textureSizeId=s.resolve("textureSize"),this.textureSize=new Float32Array(2),this.historyTextureId=s.resolve("historyTexture"),this.viewProjPrevId=s.resolve("matrix_viewProjectionPrevious"),this.viewProjInvId=s.resolve("matrix_viewProjectionInverse"),this.jittersId=s.resolve("jitters"),this.cameraParams=new Float32Array(4),this.cameraParamsId=s.resolve("camera_params"),this.setup()}}class R extends p.s{execute(){const{paramsValue:t,focusRange:e}=this;t[0]=this.focusDistance+.001,t[1]=e,t[2]=1/e,this.paramsId.setValue(t);const n=this.cameraComponent.camera;this.cameraParamsId.setValue(n.fillShaderParams(this.cameraParams)),super.execute()}constructor(t,e,n){super(t),this.cameraComponent=e,x.W.get(t,i.BzK).set("cocPS",'\n\t#include "screenDepthPS"\n\tvarying vec2 uv0;\n\tuniform vec3 params;\n\tvoid main()\n\t{\n\t\tfloat depth = getLinearScreenDepth(uv0);\n\t\tfloat focusDistance = params.x;\n\t\tfloat focusRange = params.y;\n\t\tfloat invRange = params.z;\n\t\tfloat farRange = focusDistance + focusRange * 0.5;\n\t\t\n\t\tfloat cocFar = min((depth - farRange) * invRange, 1.0);\n\t\t#ifdef NEAR_BLUR\n\t\t\tfloat nearRange = focusDistance - focusRange * 0.5;\n\t\t\tfloat cocNear = min((nearRange - depth) * invRange, 1.0);\n\t\t#else\n\t\t\tfloat cocNear = 0.0;\n\t\t#endif\n\t\tgl_FragColor = vec4(cocFar, cocNear, 0.0, 0.0);\n\t}\n'),x.W.get(t,i.g80).set("cocPS",'\n#include "screenDepthPS"\nvarying uv0: vec2f;\nuniform params: vec3f;\n@fragment\nfn fragmentMain(input: FragmentInput) -> FragmentOutput {\n\tvar output: FragmentOutput;\n\tlet depth: f32 = getLinearScreenDepth(uv0);\n\tlet focusDistance: f32 = uniform.params.x;\n\tlet focusRange: f32 = uniform.params.y;\n\tlet invRange: f32 = uniform.params.z;\n\tlet farRange: f32 = focusDistance + focusRange * 0.5;\n\tlet cocFar: f32 = min((depth - farRange) * invRange, 1.0);\n\t#ifdef NEAR_BLUR\n\t\tlet nearRange: f32 = focusDistance - focusRange * 0.5;\n\t\tvar cocNear: f32 = min((nearRange - depth) * invRange, 1.0);\n\t#else\n\t\tvar cocNear: f32 = 0.0;\n\t#endif\n\toutput.color = vec4f(cocFar, cocNear, 0.0, 0.0);\n\treturn output;\n}\n');const r=new Map;n&&r.set("NEAR_BLUR",""),m.lo.addScreenDepthChunkDefines(t,e.shaderParams,r),this.shader=m.lo.createShader(t,{uniqueName:`CocShader-${n}`,attributes:{aPosition:i.JYe},vertexChunk:"quadVS",fragmentChunk:"cocPS",fragmentDefines:r}),this.paramsId=t.scope.resolve("params"),this.paramsValue=new Float32Array(3),this.cameraParams=new Float32Array(4),this.cameraParamsId=t.scope.resolve("camera_params")}}class E{static concentric(t,e){const n=[];n.push(0,0);const r=2*Math.PI/t/e;for(let s=1;s<=t;s++){const e=s/t,o=2*Math.PI*e,i=Math.max(1,Math.floor(o/r)),a=2*Math.PI/i;for(let t=0;t<i;t++){const r=t*a,s=e*Math.cos(r),o=e*Math.sin(r);n.push(s,o)}}return n}}class w extends p.s{set blurRings(t){this._blurRings!==t&&(this._blurRings=t,this.shader=null)}get blurRings(){return this._blurRings}set blurRingPoints(t){this._blurRingPoints!==t&&(this._blurRingPoints=t,this.shader=null)}get blurRingPoints(){return this._blurRingPoints}createShader(){this.kernel=new Float32Array(E.concentric(this.blurRings,this.blurRingPoints));const t=this.kernel.length>>1,e=null!==this.nearTexture,n=new Map;n.set("{KERNEL_COUNT}",t),n.set("{INV_KERNEL_COUNT}",1/t),e&&n.set("NEAR_BLUR",""),this.shader=m.lo.createShader(this.device,{uniqueName:`DofBlurShader-${t}-${e?"nearBlur":"noNearBlur"}`,attributes:{aPosition:i.JYe},vertexChunk:"quadVS",fragmentChunk:"dofBlurPS",fragmentDefines:n})}execute(){this.shader||this.createShader(),this.nearTextureId.setValue(this.nearTexture),this.farTextureId.setValue(this.farTexture),this.cocTextureId.setValue(this.cocTexture),this.kernelId.setValue(this.kernel),this.kernelCountId.setValue(this.kernel.length>>1),this.blurRadiusNearId.setValue(this.blurRadiusNear),this.blurRadiusFarId.setValue(this.blurRadiusFar),super.execute()}constructor(t,e,n,r){super(t),this.blurRadiusNear=1,this.blurRadiusFar=1,this._blurRings=3,this._blurRingPoints=3,this.nearTexture=e,this.farTexture=n,this.cocTexture=r,x.W.get(t,i.BzK).set("dofBlurPS","\n\t#if defined(NEAR_BLUR)\n\t\tuniform sampler2D nearTexture;\n\t#endif\n\tuniform sampler2D farTexture;\n\tuniform sampler2D cocTexture;\n\tuniform vec2 kernel[{KERNEL_COUNT}];\n\tuniform float blurRadiusNear;\n\tuniform float blurRadiusFar;\n\tvarying vec2 uv0;\n\tvoid main()\n\t{\n\t\tvec2 coc = texture2D(cocTexture, uv0).rg;\n\t\tfloat cocFar = coc.r;\n\t\tvec3 sum = vec3(0.0, 0.0, 0.0);\n\t\t#if defined(NEAR_BLUR)\n\t\t\tfloat cocNear = coc.g;\n\t\t\tif (cocNear > 0.0001) {\n\t\t\t\tivec2 nearTextureSize = textureSize(nearTexture, 0);\n\t\t\t\tvec2 step = cocNear * blurRadiusNear / vec2(nearTextureSize);\n\t\t\t\tfor (int i = 0; i < {KERNEL_COUNT}; i++) {\n\t\t\t\t\tvec2 uv = uv0 + step * kernel[i];\n\t\t\t\t\tvec3 tap = texture2DLod(nearTexture, uv, 0.0).rgb;\n\t\t\t\t\tsum += tap.rgb;\n\t\t\t\t}\n\t\t\t\tsum *= float({INV_KERNEL_COUNT});\n\t\t\t} else\n\t\t#endif\n\t\t\t\n\t\t\tif (cocFar > 0.0001) {\n\t\t\tivec2 farTextureSize = textureSize(farTexture, 0);\n\t\t\tvec2 step = cocFar * blurRadiusFar / vec2(farTextureSize);\n\t\t\tfloat sumCoC = 0.0; \n\t\t\tfor (int i = 0; i < {KERNEL_COUNT}; i++) {\n\t\t\t\tvec2 uv = uv0 + step * kernel[i];\n\t\t\t\tvec3 tap = texture2DLod(farTexture, uv, 0.0).rgb;\n\t\t\t\tfloat cocThis = texture2DLod(cocTexture, uv, 0.0).r;\n\t\t\t\ttap *= cocThis;\n\t\t\t\tsumCoC += cocThis;\n\t\t\t\tsum += tap;\n\t\t\t}\n\t\t\tif (sumCoC > 0.0)\n\t\t\t\tsum /= sumCoC;\n\t\t\tsum /= cocFar;\n\t\t}\n\t\tpcFragColor0 = vec4(sum, 1.0);\n\t}\n"),x.W.get(t,i.g80).set("dofBlurPS","\n#if defined(NEAR_BLUR)\n\tvar nearTexture: texture_2d<f32>;\n\tvar nearTextureSampler: sampler;\n#endif\nvar farTexture: texture_2d<f32>;\nvar farTextureSampler: sampler;\nvar cocTexture: texture_2d<f32>;\nvar cocTextureSampler: sampler;\nuniform kernel: array<vec2f, {KERNEL_COUNT}>;\nuniform blurRadiusNear: f32;\nuniform blurRadiusFar: f32;\nvarying uv0: vec2f;\n@fragment\nfn fragmentMain(input: FragmentInput) -> FragmentOutput {\n\tvar output: FragmentOutput;\n\tlet coc: vec2f = textureSample(cocTexture, cocTextureSampler, input.uv0).rg;\n\tlet cocFar: f32 = coc.r;\n\tvar sum: vec3f = vec3f(0.0, 0.0, 0.0);\n\t#if defined(NEAR_BLUR)\n\t\tlet cocNear: f32 = coc.g;\n\t\tif (cocNear > 0.0001) {\n\t\t\tlet nearTextureSize: vec2f = vec2f(textureDimensions(nearTexture, 0));\n\t\t\tlet step: vec2f = cocNear * uniform.blurRadiusNear / nearTextureSize;\n\t\t\tfor (var i: i32 = 0; i < {KERNEL_COUNT}; i = i + 1) {\n\t\t\t\tlet uv: vec2f = uv0 + step * uniform.kernel[i].element;\n\t\t\t\tlet tap: vec3f = textureSampleLevel(nearTexture, nearTextureSampler, uv, 0.0).rgb;\n\t\t\t\tsum = sum + tap;\n\t\t\t}\n\t\t\tsum = sum * f32({INV_KERNEL_COUNT});\n\t\t} else\n\t#endif\n\t\tif (cocFar > 0.0001) {\n\t\t\tlet farTextureSize: vec2f = vec2f(textureDimensions(farTexture, 0));\n\t\t\tlet step: vec2f = cocFar * uniform.blurRadiusFar / farTextureSize;\n\t\t\tvar sumCoC: f32 = 0.0;\n\t\t\tfor (var i: i32 = 0; i < {KERNEL_COUNT}; i = i + 1) {\n\t\t\t\tlet uv: vec2f = uv0 + step * uniform.kernel[i].element;\n\t\t\t\tvar tap: vec3f = textureSampleLevel(farTexture, farTextureSampler, uv, 0.0).rgb;\n\t\t\t\tlet cocThis: f32 = textureSampleLevel(cocTexture, cocTextureSampler, uv, 0.0).r;\n\t\t\t\ttap = tap * cocThis;\n\t\t\t\tsumCoC = sumCoC + cocThis;\n\t\t\t\tsum = sum + tap;\n\t\t\t}\n\t\t\tif (sumCoC > 0.0) {\n\t\t\t\tsum = sum / sumCoC;\n\t\t\t}\n\t\t\tsum = sum / cocFar;\n\t\t}\n\toutput.color = vec4f(sum, 1.0);\n\treturn output;\n}\n");const{scope:s}=t;this.kernelId=s.resolve("kernel[0]"),this.kernelCountId=s.resolve("kernelCount"),this.blurRadiusNearId=s.resolve("blurRadiusNear"),this.blurRadiusFarId=s.resolve("blurRadiusFar"),this.nearTextureId=s.resolve("nearTexture"),this.farTextureId=s.resolve("farTexture"),this.cocTextureId=s.resolve("cocTexture")}}class D extends c.A{destroy(){this.destroyRenderPasses(),this.cocPass=null,this.farPass=null,this.blurPass=null,this.destroyRT(this.cocRT),this.destroyRT(this.farRt),this.destroyRT(this.blurRt),this.cocRT=null,this.farRt=null,this.blurRt=null}destroyRenderPasses(){for(let t=0;t<this.beforePasses.length;t++)this.beforePasses[t].destroy();this.beforePasses.length=0}destroyRT(t){t&&(t.destroyTextureBuffers(),t.destroy())}setupCocPass(t,e,n,s){const o=s?i.MqB:i.FfR;this.cocRT=this.createRenderTarget("CoCTexture",o),this.cocTexture=this.cocRT.colorBuffer;const a=new R(t,e,s);return a.init(this.cocRT,{resizeSource:n}),a.setClearColor(r.Q.BLACK),a}setupFarPass(t,e,n){this.farRt=this.createRenderTarget("FarDofTexture",e.format);const s=new g(t,e,{boxFilter:!0,premultiplyTexture:this.cocTexture,premultiplySrcChannel:"r"});return s.init(this.farRt,{resizeSource:e,scaleX:n,scaleY:n}),s.setClearColor(r.Q.BLACK),s}setupBlurPass(t,e,n,s){const o=this.farRt?.colorBuffer;this.blurRt=this.createRenderTarget("DofBlurTexture",e.format),this.blurTexture=this.blurRt.colorBuffer;const i=new w(t,n?e:null,o,this.cocTexture);return i.init(this.blurRt,{resizeSource:e,scaleX:s,scaleY:s}),i.setClearColor(r.Q.BLACK),i}createTexture(t,e){return new u.g(this.device,{name:t,width:1,height:1,format:e,mipmaps:!1,minFilter:i.DrA,magFilter:i.DrA,addressU:i.vq6,addressV:i.vq6})}createRenderTarget(t,e){return new d.O({colorBuffer:this.createTexture(t,e),depth:!1,stencil:!1})}frameUpdate(){super.frameUpdate(),this.cocPass.focusDistance=this.focusDistance,this.cocPass.focusRange=this.focusRange,this.blurPass.blurRadiusNear=this.blurRadius,this.blurPass.blurRadiusFar=this.blurRadius*(this.highQuality?1:.5),this.blurPass.blurRings=this.blurRings,this.blurPass.blurRingPoints=this.blurRingPoints}constructor(t,e,n,r,s,o){super(t),this.focusDistance=100,this.focusRange=50,this.blurRadius=1,this.blurRings=3,this.blurRingPoints=3,this.highQuality=!0,this.cocTexture=null,this.blurTexture=null,this.cocPass=null,this.farPass=null,this.blurPass=null,this.highQuality=s,this.cocPass=this.setupCocPass(t,e,n,o),this.beforePasses.push(this.cocPass);const i=s?n:r;this.farPass=this.setupFarPass(t,i,.5),this.beforePasses.push(this.farPass),this.blurPass=this.setupBlurPass(t,r,o,s?2:.5),this.beforePasses.push(this.blurPass)}}var I=n(33984);const L=[];class O extends c.A{destroy(){super.destroy(),this.renderTarget?.destroy(),this.renderTarget=null,this.linearDepthTexture?.destroy(),this.linearDepthTexture=null,this.viewBindGroups.forEach(t=>{t.defaultUniformBuffer.destroy(),t.destroy()}),this.viewBindGroups.length=0}setupRenderTarget(t){const{device:e}=this;this.linearDepthFormat=e.textureFloatRenderable?i.IQA:i.Lxj,this.linearDepthTexture=u.g.createDataTexture2D(e,"SceneLinearDepthTexture",1,1,this.linearDepthFormat);const n=new d.O({name:"PrepassRT",colorBuffer:this.linearDepthTexture,depth:!0,samples:1});this.camera.shaderParams.sceneDepthMapLinear=!0,this.init(n,t)}after(){this.device.scope.resolve("uSceneDepthMap").setValue(this.linearDepthTexture)}execute(){const{renderer:t,scene:e,renderTarget:n}=this,r=this.camera.camera,s=e.layers.layerList,o=e.layers.subLayerEnabled,i=e.layers.subLayerList;for(let a=0;a<s.length;a++){const e=s[a];if(e.id===l.$9T)break;if(e.enabled&&o[a]&&e.camerasSet.has(r)){const s=e.getCulledInstances(r),o=i[a]?s.transparent:s.opaque;for(let t=0;t<o.length;t++){const e=o[t];e.material?.depthWrite&&L.push(e)}t.renderForwardLayer(r,n,null,void 0,l.pP7,this.viewBindGroups,{meshInstances:L}),L.length=0}}}frameUpdate(){super.frameUpdate();const{camera:t}=this;let e;if(this.setClearDepth(t.clearDepthBuffer?1:void 0),t.clearDepthBuffer){const n=t.farClip-Number.MIN_VALUE;e=this.linearDepthClearValue,this.linearDepthFormat===i.IQA?e.r=n:I.A.float2RGBA8(n,e)}this.setClearColor(e)}constructor(t,e,n,s,o){super(t),this.viewBindGroups=[],this.linearDepthClearValue=new r.Q(0,0,0,0),this.scene=e,this.renderer=n,this.camera=s,this.setupRenderTarget(o)}}var A=n(60017);class F extends p.s{execute(){this.filterSizeId.setValue(4),this.sourceTextureId.setValue(this.sourceTexture);const{width:t,height:e}=this.sourceTexture;this.sourceInvResolutionValue[0]=1/t,this.sourceInvResolutionValue[1]=1/e,this.sourceInvResolutionId.setValue(this.sourceInvResolutionValue),super.execute()}constructor(t,e,n,r){super(t),this.sourceTexture=e,x.W.get(t,i.BzK).set("depthAwareBlurPS",'\n\t#include "screenDepthPS"\n\tvarying vec2 uv0;\n\tuniform sampler2D sourceTexture;\n\tuniform vec2 sourceInvResolution;\n\tuniform int filterSize;\n\tfloat random(const highp vec2 w) {\n\t\tconst vec3 m = vec3(0.06711056, 0.00583715, 52.9829189);\n\t\treturn fract(m.z * fract(dot(w, m.xy)));\n\t}\n\tmediump float bilateralWeight(in mediump float depth, in mediump float sampleDepth) {\n\t\tmediump float diff = (sampleDepth - depth);\n\t\treturn max(0.0, 1.0 - diff * diff);\n\t}\n\tvoid tap(inout float sum, inout float totalWeight, float weight, float depth, vec2 position) {\n\t\tmediump float color = texture2D(sourceTexture, position).r;\n\t\tmediump float textureDepth = -getLinearScreenDepth(position);\n\t\n\t\tmediump float bilateral = bilateralWeight(depth, textureDepth);\n\t\tbilateral *= weight;\n\t\tsum += color * bilateral;\n\t\ttotalWeight += bilateral;\n\t}\n\tvoid main() {\n\t\tmediump float depth = -getLinearScreenDepth(uv0);\n\t\tmediump float totalWeight = 1.0;\n\t\tmediump float color = texture2D(sourceTexture, uv0 ).r;\n\t\tmediump float sum = color * totalWeight;\n\t\tfor (mediump int i = -filterSize; i <= filterSize; i++) {\n\t\t\tmediump float weight = 1.0;\n\t\t\t#ifdef HORIZONTAL\n\t\t\t\tvec2 offset = vec2(i, 0) * sourceInvResolution;\n\t\t\t#else\n\t\t\t\tvec2 offset = vec2(0, i) * sourceInvResolution;\n\t\t\t#endif\n\t\t\ttap(sum, totalWeight, weight, depth, uv0 + offset);\n\t\t}\n\t\tmediump float ao = sum / totalWeight;\n\t\tgl_FragColor.r = ao;\n\t}\n'),x.W.get(t,i.g80).set("depthAwareBlurPS",'\n#include "screenDepthPS"\nvarying uv0: vec2f;\nvar sourceTexture: texture_2d<f32>;\nvar sourceTextureSampler: sampler;\nuniform sourceInvResolution: vec2f;\nuniform filterSize: i32;\nfn random(w: vec2f) -> f32 {\n\tconst m: vec3f = vec3f(0.06711056, 0.00583715, 52.9829189);\n\treturn fract(m.z * fract(dot(w, m.xy)));\n}\nfn bilateralWeight(depth: f32, sampleDepth: f32) -> f32 {\n\tlet diff: f32 = (sampleDepth - depth);\n\treturn max(0.0, 1.0 - diff * diff);\n}\nfn tap(sum_ptr: ptr<function, f32>, totalWeight_ptr: ptr<function, f32>, weight: f32, depth: f32, position: vec2f) {\n\tlet color: f32 = textureSample(sourceTexture, sourceTextureSampler, position).r;\n\tlet textureDepth: f32 = -getLinearScreenDepth(position);\n\tlet bilateral: f32 = bilateralWeight(depth, textureDepth) * weight;\n\t*sum_ptr = *sum_ptr + color * bilateral;\n\t*totalWeight_ptr = *totalWeight_ptr + bilateral;\n}\n@fragment\nfn fragmentMain(input: FragmentInput) -> FragmentOutput {\n\tvar output: FragmentOutput;\n\tlet depth: f32 = -getLinearScreenDepth(input.uv0);\n\tvar totalWeight: f32 = 1.0;\n\tlet color: f32 = textureSample(sourceTexture, sourceTextureSampler, input.uv0 ).r;\n\tvar sum: f32 = color * totalWeight;\n\tfor (var i: i32 = -uniform.filterSize; i <= uniform.filterSize; i = i + 1) {\n\t\tlet weight: f32 = 1.0;\n\t\t#ifdef HORIZONTAL\n\t\t\tvar offset: vec2f = vec2f(f32(i), 0.0) * uniform.sourceInvResolution;\n\t\t#else\n\t\t\tvar offset: vec2f = vec2f(0.0, f32(i)) * uniform.sourceInvResolution;\n\t\t#endif\n\t\ttap(&sum, &totalWeight, weight, depth, input.uv0 + offset);\n\t}\n\tlet ao: f32 = sum / totalWeight;\n\toutput.color = vec4f(ao, ao, ao, 1.0);\n\treturn output;\n}\n');const s=new Map;r&&s.set("HORIZONTAL",""),m.lo.addScreenDepthChunkDefines(t,n.shaderParams,s),this.shader=m.lo.createShader(t,{uniqueName:`DepthAware${r?"Horizontal":"Vertical"}BlurShader`,attributes:{aPosition:i.JYe},vertexChunk:"quadVS",fragmentChunk:"depthAwareBlurPS",fragmentDefines:s});const o=this.device.scope;this.sourceTextureId=o.resolve("sourceTexture"),this.sourceInvResolutionId=o.resolve("sourceInvResolution"),this.sourceInvResolutionValue=new Float32Array(2),this.filterSizeId=o.resolve("filterSize")}}class B extends p.s{destroy(){if(this.renderTarget?.destroyTextureBuffers(),this.renderTarget?.destroy(),this.renderTarget=null,this.afterPasses.length>0){const t=this.afterPasses[0].renderTarget;t?.destroyTextureBuffers(),t?.destroy()}this.afterPasses.forEach(t=>t.destroy()),this.afterPasses.length=0,super.destroy()}set scale(t){this._scale=t,this.scaleX=t,this.scaleY=t}get scale(){return this._scale}createRenderTarget(t){return new d.O({depth:!1,colorBuffer:u.g.createDataTexture2D(this.device,t,1,1,i.FfR)})}execute(){const{device:t,sourceTexture:e,sampleCount:n,minAngle:r,scale:s}=this,{width:i,height:a}=this.renderTarget.colorBuffer,l=t.scope;l.resolve("uAspect").setValue(i/a),l.resolve("uInvResolution").setValue([1/i,1/a]),l.resolve("uSampleCount").setValue([n,1/n]);const u=Math.sin(r*o.D.DEG_TO_RAD);l.resolve("uMinHorizonAngleSineSquared").setValue(u*u);const c=1/(n-.5)*10*2*3.141,h=this.radius/s,f=.1*h,d=2*f*3.141*2*this.intensity/n,v=.5*e.height;l.resolve("uSpiralTurns").setValue(10),l.resolve("uAngleIncCosSin").setValue([Math.cos(c),Math.sin(c)]),l.resolve("uMaxLevel").setValue(0),l.resolve("uInvRadiusSquared").setValue(1/(h*h)),l.resolve("uBias").setValue(.001),l.resolve("uPeak2").setValue(f*f),l.resolve("uIntensity").setValue(d),l.resolve("uPower").setValue(this.power),l.resolve("uProjectionScaleRadius").setValue(v*h),l.resolve("uRandomize").setValue(this.randomize?this._blueNoise.value():0),super.execute()}after(){this.ssaoTextureId.setValue(this.ssaoTexture);const t=this.sourceTexture;this.ssaoTextureSizeInvId.setValue([1/t.width,1/t.height])}constructor(t,e,n,s){super(t),this.radius=5,this.intensity=1,this.power=1,this.sampleCount=10,this.minAngle=5,this.randomize=!1,this._scale=1,this._blueNoise=new A.d(19),this.sourceTexture=e,this.cameraComponent=n,x.W.get(t,i.BzK).set("ssaoPS",'\n\t#include "screenDepthPS"\n\t\n\tvarying vec2 uv0;\n\tuniform vec2 uInvResolution;\n\tuniform float uAspect;\n\t#define saturate(x) clamp(x,0.0,1.0)\n\thighp float getWFromProjectionMatrix(const mat4 p, const vec3 v) {\n\t\treturn -v.z;\n\t}\n\thighp float getViewSpaceZFromW(const mat4 p, const float w) {\n\t\treturn -w;\n\t}\n\tconst float kLog2LodRate = 3.0;\n\tfloat random(const highp vec2 w) {\n\t\tconst vec3 m = vec3(0.06711056, 0.00583715, 52.9829189);\n\t\treturn fract(m.z * fract(dot(w, m.xy)));\n\t}\n\thighp vec2 getFragCoord() {\n\t\treturn gl_FragCoord.xy;\n\t}\n\thighp vec3 computeViewSpacePositionFromDepth(highp vec2 uv, highp float linearDepth) {\n\t\treturn vec3((0.5 - uv) * vec2(uAspect, 1.0) * linearDepth, linearDepth);\n\t}\n\thighp vec3 faceNormal(highp vec3 dpdx, highp vec3 dpdy) {\n\t\treturn normalize(cross(dpdx, dpdy));\n\t}\n\thighp vec3 computeViewSpaceNormal(const highp vec3 position) {\n\t\treturn faceNormal(dFdx(position), dFdy(position));\n\t}\n\thighp vec3 computeViewSpaceNormal(const highp vec3 position, const highp vec2 uv) {\n\t\thighp vec2 uvdx = uv + vec2(uInvResolution.x, 0.0);\n\t\thighp vec2 uvdy = uv + vec2(0.0, uInvResolution.y);\n\t\thighp vec3 px = computeViewSpacePositionFromDepth(uvdx, -getLinearScreenDepth(uvdx));\n\t\thighp vec3 py = computeViewSpacePositionFromDepth(uvdy, -getLinearScreenDepth(uvdy));\n\t\thighp vec3 dpdx = px - position;\n\t\thighp vec3 dpdy = py - position;\n\t\treturn faceNormal(dpdx, dpdy);\n\t}\n\tuniform vec2 uSampleCount;\n\tuniform float uSpiralTurns;\n\t#define PI (3.14159)\n\tmediump vec3 tapLocation(mediump float i, const mediump float noise) {\n\t\tmediump float offset = ((2.0 * PI) * 2.4) * noise;\n\t\tmediump float angle = ((i * uSampleCount.y) * uSpiralTurns) * (2.0 * PI) + offset;\n\t\tmediump float radius = (i + noise + 0.5) * uSampleCount.y;\n\t\treturn vec3(cos(angle), sin(angle), radius * radius);\n\t}\n\thighp vec2 startPosition(const float noise) {\n\t\tfloat angle = ((2.0 * PI) * 2.4) * noise;\n\t\treturn vec2(cos(angle), sin(angle));\n\t}\n\tuniform vec2 uAngleIncCosSin;\n\thighp mat2 tapAngleStep() {\n\t\thighp vec2 t = uAngleIncCosSin;\n\t\treturn mat2(t.x, t.y, -t.y, t.x);\n\t}\n\tmediump vec3 tapLocationFast(mediump float i, mediump vec2 p, const mediump float noise) {\n\t\tmediump float radius = (i + noise + 0.5) * uSampleCount.y;\n\t\treturn vec3(p, radius * radius);\n\t}\n\tuniform float uMaxLevel;\n\tuniform float uInvRadiusSquared;\n\tuniform float uMinHorizonAngleSineSquared;\n\tuniform float uBias;\n\tuniform float uPeak2;\n\tvoid computeAmbientOcclusionSAO(inout mediump float occlusion, mediump float i, mediump float ssDiskRadius,\n\t\t\tconst highp vec2 uv, const highp vec3 origin, const mediump vec3 normal,\n\t\t\tconst mediump vec2 tapPosition, const float noise) {\n\t\tmediump vec3 tap = tapLocationFast(i, tapPosition, noise);\n\t\tmediump float ssRadius = max(1.0, tap.z * ssDiskRadius);\n\t\tmediump vec2 uvSamplePos = uv + vec2(ssRadius * tap.xy) * uInvResolution;\n\t\tmediump float level = clamp(floor(log2(ssRadius)) - kLog2LodRate, 0.0, float(uMaxLevel));\n\t\thighp float occlusionDepth = -getLinearScreenDepth(uvSamplePos);\n\t\thighp vec3 p = computeViewSpacePositionFromDepth(uvSamplePos, occlusionDepth);\n\t\tvec3 v = p - origin;\n\t\tfloat vv = dot(v, v);\n\t\tfloat vn = dot(v, normal);\n\t\tmediump float w = max(0.0, 1.0 - vv * uInvRadiusSquared);\n\t\tw = w * w;\n\t\tw *= step(vv * uMinHorizonAngleSineSquared, vn * vn);\n\t\tocclusion += w * max(0.0, vn + origin.z * uBias) / (vv + uPeak2);\n\t}\n\tuniform float uProjectionScaleRadius;\n\tuniform float uIntensity;\n\tuniform float uRandomize;\n\tfloat scalableAmbientObscurance(highp vec2 uv, highp vec3 origin, vec3 normal) {\n\t\tfloat noise = random(getFragCoord()) + uRandomize;\n\t\thighp vec2 tapPosition = startPosition(noise);\n\t\thighp mat2 angleStep = tapAngleStep();\n\t\tfloat ssDiskRadius = -(uProjectionScaleRadius / origin.z);\n\t\tfloat occlusion = 0.0;\n\t\tfor (float i = 0.0; i < uSampleCount.x; i += 1.0) {\n\t\t\tcomputeAmbientOcclusionSAO(occlusion, i, ssDiskRadius, uv, origin, normal, tapPosition, noise);\n\t\t\ttapPosition = angleStep * tapPosition;\n\t\t}\n\t\treturn occlusion;\n\t}\n\tuniform float uPower;\n\tvoid main() {\n\t\thighp vec2 uv = uv0;\n\t\thighp float depth = -getLinearScreenDepth(uv0);\n\t\thighp vec3 origin = computeViewSpacePositionFromDepth(uv, depth);\n\t\tvec3 normal = computeViewSpaceNormal(origin, uv);\n\t\tfloat occlusion = 0.0;\n\t\tif (uIntensity > 0.0) {\n\t\t\tocclusion = scalableAmbientObscurance(uv, origin, normal);\n\t\t}\n\t\tfloat ao = max(0.0, 1.0 - occlusion * uIntensity);\n\t\tao = pow(ao, uPower);\n\t\tgl_FragColor = vec4(ao, ao, ao, 1.0);\n\t}\n'),x.W.get(t,i.g80).set("ssaoPS",'\n\t#include "screenDepthPS"\n\tvarying uv0: vec2f;\n\tuniform uInvResolution: vec2f;\n\tuniform uAspect: f32;\n\tfn getWFromProjectionMatrix(p: mat4x4f, v: vec3f) -> f32 {\n\t\treturn -v.z;\n\t}\n\tfn getViewSpaceZFromW(p: mat4x4f, w: f32) -> f32 {\n\t\treturn -w;\n\t}\n\tconst kLog2LodRate: f32 = 3.0;\n\tfn random(w: vec2f) -> f32 {\n\t\tconst m: vec3f = vec3f(0.06711056, 0.00583715, 52.9829189);\n\t\treturn fract(m.z * fract(dot(w, m.xy)));\n\t}\n\tfn getFragCoord() -> vec2f {\n\t\treturn pcPosition.xy;\n\t}\n\tfn computeViewSpacePositionFromDepth(uv: vec2f, linearDepth: f32) -> vec3f {\n\t\treturn vec3f((0.5 - uv) * vec2f(uniform.uAspect, 1.0) * linearDepth, linearDepth);\n\t}\n\tfn faceNormal(dpdx: vec3f, dpdy: vec3f) -> vec3f {\n\t\treturn normalize(cross(dpdx, dpdy));\n\t}\n\tfn computeViewSpaceNormalDeriv(position: vec3f) -> vec3f {\n\t\treturn faceNormal(dpdx(position), dpdy(position));\n\t}\n\tfn computeViewSpaceNormalDepth(position: vec3f, uv: vec2f) -> vec3f {\n\t\tlet uvdx: vec2f = uv + vec2f(uniform.uInvResolution.x, 0.0);\n\t\tlet uvdy: vec2f = uv + vec2f(0.0, uniform.uInvResolution.y);\n\t\tlet px: vec3f = computeViewSpacePositionFromDepth(uvdx, -getLinearScreenDepth(uvdx));\n\t\tlet py: vec3f = computeViewSpacePositionFromDepth(uvdy, -getLinearScreenDepth(uvdy));\n\t\tlet dpdx: vec3f = px - position;\n\t\tlet dpdy: vec3f = py - position;\n\t\treturn faceNormal(dpdx, dpdy);\n\t}\n\tuniform uSampleCount: vec2f;\n\tuniform uSpiralTurns: f32;\n\tconst PI: f32 = 3.14159;\n\tfn tapLocation(i: f32, noise: f32) -> vec3f {\n\t\tlet offset: f32 = ((2.0 * PI) * 2.4) * noise;\n\t\tlet angle: f32 = ((i * uniform.uSampleCount.y) * uniform.uSpiralTurns) * (2.0 * PI) + offset;\n\t\tlet radius: f32 = (i + noise + 0.5) * uniform.uSampleCount.y;\n\t\treturn vec3f(cos(angle), sin(angle), radius * radius);\n\t}\n\tfn startPosition(noise: f32) -> vec2f {\n\t\tlet angle: f32 = ((2.0 * PI) * 2.4) * noise;\n\t\treturn vec2f(cos(angle), sin(angle));\n\t}\n\tuniform uAngleIncCosSin: vec2f;\n\tfn tapAngleStep() -> mat2x2f {\n\t\tlet t: vec2f = uniform.uAngleIncCosSin;\n\t\treturn mat2x2f(vec2f(t.x, t.y), vec2f(-t.y, t.x));\n\t}\n\tfn tapLocationFast(i: f32, p: vec2f, noise_in: f32) -> vec3f {\n\t\tlet radius: f32 = (i + noise_in + 0.5) * uniform.uSampleCount.y;\n\t\treturn vec3f(p.x, p.y, radius * radius);\n\t}\n\tuniform uMaxLevel: f32;\n\tuniform uInvRadiusSquared: f32;\n\tuniform uMinHorizonAngleSineSquared: f32;\n\tuniform uBias: f32;\n\tuniform uPeak2: f32;\n\tfn computeAmbientOcclusionSAO(occlusion_ptr: ptr<function, f32>, i: f32, ssDiskRadius: f32,\n\t\t\tuv: vec2f, origin: vec3f, normal: vec3f,\n\t\t\ttapPosition: vec2f, noise: f32) {\n\t\tlet tap: vec3f = tapLocationFast(i, tapPosition, noise);\n\t\tlet ssRadius: f32 = max(1.0, tap.z * ssDiskRadius);\n\t\tlet uvSamplePos: vec2f = uv + (ssRadius * tap.xy) * uniform.uInvResolution;\n\t\tlet level: f32 = clamp(floor(log2(ssRadius)) - kLog2LodRate, 0.0, uniform.uMaxLevel);\n\t\tlet occlusionDepth: f32 = -getLinearScreenDepth(uvSamplePos);\n\t\tlet p: vec3f = computeViewSpacePositionFromDepth(uvSamplePos, occlusionDepth);\n\t\tlet v: vec3f = p - origin;\n\t\tlet vv: f32 = dot(v, v);\n\t\tlet vn: f32 = dot(v, normal);\n\t\tvar w_val: f32 = max(0.0, 1.0 - vv * uniform.uInvRadiusSquared);\n\t\tw_val = w_val * w_val;\n\t\tw_val = w_val * step(vv * uniform.uMinHorizonAngleSineSquared, vn * vn);\n\t\t*occlusion_ptr = *occlusion_ptr + w_val * max(0.0, vn + origin.z * uniform.uBias) / (vv + uniform.uPeak2);\n\t}\n\tuniform uProjectionScaleRadius: f32;\n\tuniform uIntensity: f32;\n\tuniform uRandomize: f32;\n\tfn scalableAmbientObscurance(uv: vec2f, origin: vec3f, normal: vec3f) -> f32 {\n\t\tlet noise: f32 = random(getFragCoord()) + uniform.uRandomize;\n\t\tvar tapPosition: vec2f = startPosition(noise);\n\t\tlet angleStep: mat2x2f = tapAngleStep();\n\t\tlet ssDiskRadius: f32 = -(uniform.uProjectionScaleRadius / origin.z);\n\t\tvar occlusion: f32 = 0.0;\n\t\tfor (var i: i32 = 0; i < i32(uniform.uSampleCount.x); i = i + 1) {\n\t\t\tcomputeAmbientOcclusionSAO(&occlusion, f32(i), ssDiskRadius, uv, origin, normal, tapPosition, noise);\n\t\t\ttapPosition = angleStep * tapPosition;\n\t\t}\n\t\treturn occlusion;\n\t}\n\tuniform uPower: f32;\n\t@fragment\n\tfn fragmentMain(input: FragmentInput) -> FragmentOutput {\n\t\tvar output: FragmentOutput;\n\t\tlet uv: vec2f = input.uv0;\n\t\tlet depth: f32 = -getLinearScreenDepth(input.uv0);\n\t\tlet origin: vec3f = computeViewSpacePositionFromDepth(uv, depth);\n\t\tlet normal: vec3f = computeViewSpaceNormalDepth(origin, uv);\n\t\tvar occlusion: f32 = 0.0;\n\t\tif (uniform.uIntensity > 0.0) {\n\t\t\tocclusion = scalableAmbientObscurance(uv, origin, normal);\n\t\t}\n\t\tvar ao: f32 = max(0.0, 1.0 - occlusion * uniform.uIntensity);\n\t\tao = pow(ao, uniform.uPower);\n\t\toutput.color = vec4f(ao, ao, ao, 1.0);\n\t\treturn output;\n\t}\n');const o=new Map;m.lo.addScreenDepthChunkDefines(t,n.shaderParams,o),this.shader=m.lo.createShader(t,{uniqueName:"SsaoShader",attributes:{aPosition:i.JYe},vertexChunk:"quadVS",fragmentChunk:"ssaoPS",fragmentDefines:o});const a=this.createRenderTarget("SsaoFinalTexture");this.ssaoTexture=a.colorBuffer,this.init(a,{resizeSource:this.sourceTexture});const l=new r.Q(0,0,0,0);if(this.setClearColor(l),s){const e=this.createRenderTarget("SsaoTempTexture"),r=new F(t,a.colorBuffer,n,!0);r.init(e,{resizeSource:a.colorBuffer}),r.setClearColor(l);const s=new F(t,e.colorBuffer,n,!1);s.init(a,{resizeSource:a.colorBuffer}),s.setClearColor(l),this.afterPasses.push(r),this.afterPasses.push(s)}this.ssaoTextureId=t.scope.resolve("ssaoTexture"),this.ssaoTextureSizeInvId=t.scope.resolve("ssaoTextureSizeInv")}}class U{constructor(){this.stencil=!1,this.samples=1,this.sceneColorMap=!1,this.lastGrabLayerId=l.CTD,this.lastGrabLayerIsTransparent=!1,this.lastSceneLayerId=l.t0Z,this.lastSceneLayerIsTransparent=!0,this.taaEnabled=!1,this.bloomEnabled=!1,this.ssaoType=a,this.ssaoBlurEnabled=!0,this.prepassEnabled=!1,this.dofEnabled=!1,this.dofNearBlur=!1,this.dofHighQuality=!0}}const z=new U;class M extends c.A{destroy(){this.reset()}reset(){this.sceneTexture=null,this.sceneTextureHalf=null,this.rt&&(this.rt.destroyTextureBuffers(),this.rt.destroy(),this.rt=null),this.rtHalf&&(this.rtHalf.destroyTextureBuffers(),this.rtHalf.destroy(),this.rtHalf=null),this.beforePasses.forEach(t=>t.destroy()),this.beforePasses.length=0,this.prePass=null,this.scenePass=null,this.scenePassTransparent=null,this.colorGrabPass=null,this.composePass=null,this.bloomPass=null,this.ssaoPass=null,this.taaPass=null,this.afterPass=null,this.scenePassHalf=null,this.dofPass=null}sanitizeOptions(t){return((t=Object.assign({},z,t)).taaEnabled||t.ssaoType!==a||t.dofEnabled)&&(t.prepassEnabled=!0),t}set renderTargetScale(t){this._renderTargetScale=t,this.scenePass&&(this.scenePass.scaleX=t,this.scenePass.scaleY=t)}get renderTargetScale(){return this._renderTargetScale}needsReset(t){const e=this.options;return t.ssaoType!==e.ssaoType||t.ssaoBlurEnabled!==e.ssaoBlurEnabled||t.taaEnabled!==e.taaEnabled||t.samples!==e.samples||t.stencil!==e.stencil||t.bloomEnabled!==e.bloomEnabled||t.prepassEnabled!==e.prepassEnabled||t.sceneColorMap!==e.sceneColorMap||t.dofEnabled!==e.dofEnabled||t.dofNearBlur!==e.dofNearBlur||t.dofHighQuality!==e.dofHighQuality||(n=t.formats,r=e.formats,!(n===r||Array.isArray(n)&&Array.isArray(r)&&n.length===r.length&&n.every((t,e)=>t===r[e])));var n,r}update(t){t=this.sanitizeOptions(t),(this.needsReset(t)||this.layersDirty)&&(this.layersDirty=!1,this.reset()),this.options=t,this.sceneTexture||this.setupRenderPasses(this.options)}createRenderTarget(t,e,n,r,s){const o=new u.g(this.device,{name:t,width:4,height:4,format:this.hdrFormat,mipmaps:!1,minFilter:i.DrA,magFilter:i.DrA,addressU:i.vq6,addressV:i.vq6});return new d.O({colorBuffer:o,depth:e,stencil:n,samples:r,flipY:s})}setupRenderPasses(t){const{device:e}=this,n=this.cameraComponent,r=n.renderTarget;this.hdrFormat=e.getRenderableHdrFormat(t.formats,!0,t.samples)||i.Lxj,this._bloomEnabled=t.bloomEnabled&&this.hdrFormat!==i.Lxj,this._sceneHalfEnabled=this._bloomEnabled||t.dofEnabled,n.shaderParams.ssaoEnabled="lighting"===t.ssaoType;const s=!!r?.flipY;this.rt=this.createRenderTarget("SceneColor",!0,t.stencil,t.samples,s),this.sceneTexture=this.rt.colorBuffer,this._sceneHalfEnabled&&(this.rtHalf=this.createRenderTarget("SceneColorHalf",!1,!1,1,s),this.sceneTextureHalf=this.rtHalf.colorBuffer),this.sceneOptions={resizeSource:r,scaleX:this.renderTargetScale,scaleY:this.renderTargetScale},this.createPasses(t);const o=this.collectPasses();this.beforePasses=o.filter(t=>null!=t)}collectPasses(){return[this.prePass,this.ssaoPass,this.scenePass,this.colorGrabPass,this.scenePassTransparent,this.taaPass,this.scenePassHalf,this.bloomPass,this.dofPass,this.composePass,this.afterPass]}createPasses(t){this.setupScenePrepass(t),this.setupSsaoPass(t);const e=this.setupScenePass(t),n=this.setupTaaPass(t);this.setupSceneHalfPass(t,n),this.setupBloomPass(t,this.sceneTextureHalf),this.setupDofPass(t,this.sceneTexture,this.sceneTextureHalf),this.setupComposePass(t),this.setupAfterPass(t,e)}setupScenePrepass(t){if(t.prepassEnabled){const{app:t,device:e,cameraComponent:n}=this,{scene:r,renderer:s}=t;this.prePass=new O(e,r,s,n,this.sceneOptions)}}setupScenePassSettings(t){t.gammaCorrection=l.hcE,t.toneMapping=l.YJk}setupScenePass(t){const{app:e,device:n,cameraComponent:r}=this,{scene:s,renderer:o}=e,i=s.layers;this.scenePass=new f.r(n,i,s,o),this.setupScenePassSettings(this.scenePass),this.scenePass.init(this.rt,this.sceneOptions);const a=t.sceneColorMap?t.lastGrabLayerId:t.lastSceneLayerId,l=t.sceneColorMap?t.lastGrabLayerIsTransparent:t.lastSceneLayerIsTransparent,u={lastAddedIndex:0,clearRenderTarget:!0};return u.lastAddedIndex=this.scenePass.addLayers(i,r,u.lastAddedIndex,u.clearRenderTarget,a,l),u.clearRenderTarget=!1,t.sceneColorMap&&(this.colorGrabPass=new h.j(n),this.colorGrabPass.source=this.rt,this.scenePassTransparent=new f.r(n,i,s,o),this.setupScenePassSettings(this.scenePassTransparent),this.scenePassTransparent.init(this.rt),u.lastAddedIndex=this.scenePassTransparent.addLayers(i,r,u.lastAddedIndex,u.clearRenderTarget,t.lastSceneLayerId,t.lastSceneLayerIsTransparent),this.scenePassTransparent.rendersAnything||(this.scenePassTransparent.destroy(),this.scenePassTransparent=null),this.scenePassTransparent&&t.prepassEnabled&&(this.scenePassTransparent.depthStencilOps.storeDepth=!0)),u}setupSsaoPass(t){const{ssaoBlurEnabled:e,ssaoType:n}=t,{device:r,cameraComponent:s}=this;n!==a&&(this.ssaoPass=new B(r,this.sceneTexture,s,e))}setupSceneHalfPass(t,e){this._sceneHalfEnabled&&(this.scenePassHalf=new g(this.device,this.sceneTexture,{boxFilter:!0,removeInvalid:!0}),this.scenePassHalf.name="RenderPassSceneHalf",this.scenePassHalf.init(this.rtHalf,{resizeSource:e,scaleX:.5,scaleY:.5}),this.scenePassHalf.setClearColor(r.Q.BLACK))}setupBloomPass(t,e){this._bloomEnabled&&(this.bloomPass=new b(this.device,e,this.hdrFormat))}setupDofPass(t,e,n){t.dofEnabled&&(this.dofPass=new D(this.device,this.cameraComponent,e,n,t.dofHighQuality,t.dofNearBlur))}setupTaaPass(t){let e=this.sceneTexture;return t.taaEnabled&&(this.taaPass=new _(this.device,this.sceneTexture,this.cameraComponent),e=this.taaPass.historyTexture),e}setupComposePass(t){this.composePass=new C(this.device),this.composePass.bloomTexture=this.bloomPass?.bloomTexture,this.composePass.hdrScene=this.hdrFormat!==i.Lxj,this.composePass.taaEnabled=t.taaEnabled,this.composePass.cocTexture=this.dofPass?.cocTexture,this.composePass.blurTexture=this.dofPass?.blurTexture,this.composePass.blurTextureUpscale=!this.dofPass?.highQuality;const e=this.cameraComponent.renderTarget;this.composePass.init(e),this.composePass.ssaoTexture="combine"===t.ssaoType?this.ssaoPass.ssaoTexture:null}setupAfterPass(t,e){const{app:n,cameraComponent:r}=this,{scene:s,renderer:o}=n,i=s.layers,a=r.renderTarget;this.afterPass=new f.r(this.device,i,s,o),this.afterPass.init(a),this.afterPass.addLayers(i,r,e.lastAddedIndex,e.clearRenderTarget)}frameUpdate(){this.layersDirty&&this.cameraFrame.update(),super.frameUpdate();const t=this.taaPass?.update()??this.rt.colorBuffer;this.composePass.sceneTexture=t,this.scenePassHalf?.setSourceTexture(t)}constructor(t,e,n,r={}){super(t.graphicsDevice),this._renderTargetScale=1,this.layersDirty=!1,this.rt=null,this.app=t,this.cameraComponent=n,this.cameraFrame=e,this.options=this.sanitizeOptions(r),this.setupRenderPasses(this.options)}}class V{destroy(){this.disable(),this.cameraLayersChanged.off()}enable(){this.renderPassCamera=this.createRenderPass(),this.cameraComponent.renderPasses=[this.renderPassCamera]}disable(){const t=this.cameraComponent;t.renderPasses?.forEach(t=>{t.destroy()}),t.renderPasses=[],t.rendering=null,t.jitter=0,t.shaderParams.ssaoEnabled=!1,this.renderPassCamera=null}createRenderPass(){return new M(this.app,this,this.cameraComponent,this.options)}set enabled(t){this._enabled!==t&&(t?this.enable():this.disable(),this._enabled=t)}get enabled(){return this._enabled}updateOptions(){const{options:t,rendering:e,bloom:n,taa:r,ssao:s}=this;t.stencil=e.stencil,t.samples=e.samples,t.sceneColorMap=e.sceneColorMap,t.prepassEnabled=e.sceneDepthMap,t.bloomEnabled=n.intensity>0,t.taaEnabled=r.enabled,t.ssaoType=s.type,t.ssaoBlurEnabled=s.blurEnabled,t.formats=e.renderFormats.slice(),t.dofEnabled=this.dof.enabled,t.dofNearBlur=this.dof.nearBlur,t.dofHighQuality=this.dof.highQuality}update(){if(!this._enabled)return;const t=this.cameraComponent,{options:e,renderPassCamera:n,rendering:r,bloom:s,grading:i,colorEnhance:l,vignette:u,fringing:c,taa:h,ssao:f}=this;this.updateOptions(),n.update(e);const{composePass:d,bloomPass:v,ssaoPass:p,dofPass:m}=n;n.renderTargetScale=o.D.clamp(r.renderTargetScale,.1,1),d.toneMapping=r.toneMapping,d.sharpness=r.sharpness,e.bloomEnabled&&v&&(d.bloomIntensity=s.intensity,v.blurLevel=s.blurLevel),e.dofEnabled&&(m.focusDistance=this.dof.focusDistance,m.focusRange=this.dof.focusRange,m.blurRadius=this.dof.blurRadius,m.blurRings=this.dof.blurRings,m.blurRingPoints=this.dof.blurRingPoints),e.ssaoType!==a&&(p.intensity=f.intensity,p.power=f.power,p.radius=f.radius,p.sampleCount=f.samples,p.minAngle=f.minAngle,p.scale=f.scale,p.randomize=f.randomize),d.gradingEnabled=i.enabled,i.enabled&&(d.gradingSaturation=i.saturation,d.gradingBrightness=i.brightness,d.gradingContrast=i.contrast,d.gradingTint=i.tint),d.colorLUT=this.colorLUT.texture,d.colorLUTIntensity=this.colorLUT.intensity,d.vignetteEnabled=u.intensity>0,d.vignetteEnabled&&(d.vignetteInner=u.inner,d.vignetteOuter=u.outer,d.vignetteCurvature=u.curvature,d.vignetteIntensity=u.intensity,d.vignetteColor.copy(u.color)),d.fringingEnabled=c.intensity>0,d.fringingEnabled&&(d.fringingIntensity=c.intensity),d.colorEnhanceEnabled=l.enabled,l.enabled&&(d.colorEnhanceShadows=l.shadows,d.colorEnhanceHighlights=l.highlights,d.colorEnhanceVibrance=l.vibrance,d.colorEnhanceMidtones=l.midtones,d.colorEnhanceDehaze=l.dehaze),t.jitter=h.enabled?h.jitter:0,d.debug=this.debug,"ssao"===d.debug&&e.ssaoType===a&&(d.debug=null),"vignette"!==d.debug||d.vignetteEnabled||(d.debug=null)}constructor(t,e){this._enabled=!0,this.rendering={renderFormats:[i.s_z,i.WBN,i.s7h],stencil:!1,renderTargetScale:1,samples:1,sceneColorMap:!1,sceneDepthMap:!1,toneMapping:0,sharpness:0},this.ssao={type:a,blurEnabled:!0,randomize:!1,intensity:.5,radius:30,samples:12,power:6,minAngle:10,scale:1},this.bloom={intensity:0,blurLevel:16},this.grading={enabled:!1,brightness:1,contrast:1,saturation:1,tint:new r.Q(1,1,1,1)},this.colorLUT={texture:null,intensity:1},this.vignette={intensity:0,inner:.5,outer:1,curvature:.5,color:new r.Q(0,0,0)},this.taa={enabled:!1,jitter:1},this.fringing={intensity:0},this.colorEnhance={enabled:!1,shadows:0,highlights:0,vibrance:0,midtones:0,dehaze:0},this.dof={enabled:!1,nearBlur:!1,focusDistance:100,focusRange:10,blurRadius:3,blurRings:4,blurRingPoints:5,highQuality:!0},this.debug=null,this.options=new U,this.renderPassCamera=null,this.app=t,this.cameraComponent=e,this.updateOptions(),this.enable(),this.cameraLayersChanged=e.on("set:layers",()=>{this.renderPassCamera&&(this.renderPassCamera.layersDirty=!0)})}}const N=0,k="none",W=18,G=12,j=14,H="none";class q{renderFormat=W;renderFormatFallback0=G;renderFormatFallback1=j;stencil=!1;renderTargetScale=1;samples=1;sceneColorMap=!1;sceneDepthMap=!1;toneMapping=N;sharpness=0;debug=H}class Q{type=k;blurEnabled=!0;intensity=.5;radius=30;samples=12;power=6;minAngle=10;scale=1}class ${enabled=!1;intensity=.01;blurLevel=16}class X{enabled=!1;brightness=1;contrast=1;saturation=1;tint=new r.Q(1,1,1,1)}class Y{texture=null;intensity=1}class K{enabled=!1;intensity=.5;inner=.5;outer=1;curvature=.5;color=new r.Q(0,0,0,1)}class Z{enabled=!1;intensity=50}class J{enabled=!1;shadows=0;highlights=0;midtones=0;vibrance=0;dehaze=0}class tt{enabled=!1;jitter=1}class et{enabled=!1;highQuality=!0;nearBlur=!1;focusDistance=100;focusRange=10;blurRadius=3;blurRings=4;blurRingPoints=5}class nt extends s.e{static scriptName="cameraFrame";rendering=new q;ssao=new Q;bloom=new $;grading=new X;colorLUT=new Y;vignette=new K;taa=new tt;fringing=new Z;colorEnhance=new J;dof=new et;engineCameraFrame;initialize(){this.engineCameraFrame=new V(this.app,this.entity.camera),this.on("enable",()=>{this.engineCameraFrame.enabled=!0}),this.on("disable",()=>{this.engineCameraFrame.enabled=!1}),this.on("destroy",()=>{this.engineCameraFrame.destroy()}),this.on("state",t=>{this.engineCameraFrame.enabled=t})}postUpdate(t){const e=this.engineCameraFrame,{rendering:n,bloom:r,grading:s,colorEnhance:o,vignette:i,fringing:a,taa:l,ssao:u,dof:c,colorLUT:h}=this,f=e.rendering;f.renderFormats.length=0,f.renderFormats.push(n.renderFormat),f.renderFormats.push(n.renderFormatFallback0),f.renderFormats.push(n.renderFormatFallback1),f.stencil=n.stencil,f.renderTargetScale=n.renderTargetScale,f.samples=n.samples,f.sceneColorMap=n.sceneColorMap,f.sceneDepthMap=n.sceneDepthMap,f.toneMapping=n.toneMapping,f.sharpness=n.sharpness;const d=e.ssao;d.type=u.type,u.type!==k&&(d.intensity=u.intensity,d.radius=u.radius,d.samples=u.samples,d.power=u.power,d.minAngle=u.minAngle,d.scale=u.scale);const v=e.bloom;v.intensity=r.enabled?r.intensity:0,r.enabled&&(v.blurLevel=r.blurLevel);const p=e.grading;p.enabled=s.enabled,s.enabled&&(p.brightness=s.brightness,p.contrast=s.contrast,p.saturation=s.saturation,p.tint.copy(s.tint));const m=e.colorLUT;h.texture?.resource?(m.texture=h.texture.resource,m.intensity=h.intensity):m.texture=null;const x=e.vignette;x.intensity=i.enabled?i.intensity:0,i.enabled&&(x.inner=i.inner,x.outer=i.outer,x.curvature=i.curvature,x.color.copy(i.color));const g=e.taa;g.enabled=l.enabled,l.enabled&&(g.jitter=l.jitter);e.fringing.intensity=a.enabled?a.intensity:0;const y=e.colorEnhance;y.enabled=o.enabled,o.enabled&&(y.shadows=o.shadows,y.highlights=o.highlights,y.midtones=o.midtones,y.vibrance=o.vibrance,y.dehaze=o.dehaze);const b=e.dof;b.enabled=c.enabled,c.enabled&&(b.highQuality=c.highQuality,b.nearBlur=c.nearBlur,b.focusDistance=c.focusDistance,b.focusRange=c.focusRange,b.blurRadius=c.blurRadius,b.blurRings=c.blurRings,b.blurRingPoints=c.blurRingPoints),e.debug=n.debug,e.update()}}},78692(t,e,n){n.d(e,{x:()=>d});var r=n(48355),s=n(40050),o=n(91622),i=n(27351),a=n(70917),l=n(30198),u=n(1002),c=n(53322),h=n(85169);const f=new s.Z;class d extends h.e{static scriptName="grid";static RESOLUTION_LOW=0;static RESOLUTION_MEDIUM=1;static RESOLUTION_HIGH=2;_material;_meshInstance;_halfExtents=new s.Z;_colorX=new r.Q(1,.3,.3);_colorZ=new r.Q(.3,.3,1);_resolution=d.RESOLUTION_HIGH;initialize(){if(this.entity.render)return void console.error("The entity already has a render component.");this.entity.addComponent("render",{castShadows:!1}),this._material=new u.B({uniqueName:"grid-shader",vertexGLSL:"\n    attribute vec3 vertex_position;\n    attribute vec2 aUv0;\n\n    uniform mat4 matrix_model;\n    uniform mat4 matrix_viewProjection;\n\n    varying vec2 uv0;\n\n    void main(void) {\n        gl_Position = matrix_viewProjection * matrix_model * vec4(vertex_position, 1.0);\n        uv0 = aUv0;\n    }\n",fragmentGLSL:"\n    uniform vec2 uHalfExtents;\n    uniform vec3 uColorX;\n    uniform vec3 uColorZ;\n    uniform int uResolution;\n\n    varying vec2 uv0;\n\n    // https://bgolus.medium.com/the-best-darn-grid-shader-yet-727f9278b9d8#1e7c\n    float pristineGrid(in vec2 uv, in vec2 ddx, in vec2 ddy, vec2 lineWidth) {\n        vec2 uvDeriv = vec2(length(vec2(ddx.x, ddy.x)), length(vec2(ddx.y, ddy.y)));\n        bvec2 invertLine = bvec2(lineWidth.x > 0.5, lineWidth.y > 0.5);\n        vec2 targetWidth = vec2(\n            invertLine.x ? 1.0 - lineWidth.x : lineWidth.x,\n            invertLine.y ? 1.0 - lineWidth.y : lineWidth.y\n        );\n        vec2 drawWidth = clamp(targetWidth, uvDeriv, vec2(0.5));\n        vec2 lineAA = uvDeriv * 1.5;\n        vec2 gridUV = abs(fract(uv) * 2.0 - 1.0);\n        gridUV.x = invertLine.x ? gridUV.x : 1.0 - gridUV.x;\n        gridUV.y = invertLine.y ? gridUV.y : 1.0 - gridUV.y;\n        vec2 grid2 = smoothstep(drawWidth + lineAA, drawWidth - lineAA, gridUV);\n\n        grid2 *= clamp(targetWidth / drawWidth, 0.0, 1.0);\n        grid2 = mix(grid2, targetWidth, clamp(uvDeriv * 2.0 - 1.0, 0.0, 1.0));\n        grid2.x = invertLine.x ? 1.0 - grid2.x : grid2.x;\n        grid2.y = invertLine.y ? 1.0 - grid2.y : grid2.y;\n\n        return mix(grid2.x, 1.0, grid2.y);\n    }\n\n    void main(void) {\n        vec2 uv = uv0;\n\n        vec2 pos = (uv * 2.0 - 1.0) * uHalfExtents;\n        vec2 ddx = dFdx(pos);\n        vec2 ddy = dFdy(pos);\n\n        float epsilon = 1.0 / 255.0;\n\n        vec2 levelPos;\n        float levelSize;\n        float levelAlpha;\n\n        levelPos = pos * 0.1;\n        levelSize = 2.0 / 1000.0;\n        levelAlpha = pristineGrid(levelPos, ddx * 0.1, ddy * 0.1, vec2(levelSize));\n        if (levelAlpha > epsilon) {\n            vec3 color;\n            if (abs(levelPos.x) < levelSize) {\n                if (abs(levelPos.y) < levelSize) {\n                    color = vec3(1.0);\n                } else {\n                    color = uColorZ;\n                }\n            } else if (abs(levelPos.y) < levelSize) {\n                color = uColorX;\n            } else {\n                color = vec3(0.9);\n            }\n            gl_FragColor = vec4(color, levelAlpha);\n            return;\n        }\n\n        levelPos = pos;\n        levelSize = 1.0 / 100.0;\n        levelAlpha = pristineGrid(levelPos, ddx, ddy, vec2(levelSize));\n        if (levelAlpha > epsilon) {\n            if (uResolution < 1) {\n                discard;\n            }\n            gl_FragColor = vec4(vec3(0.7), levelAlpha);\n            return;\n        }\n\n        levelPos = pos * 10.0;\n        levelSize = 1.0 / 100.0;\n        levelAlpha = pristineGrid(levelPos, ddx * 10.0, ddy * 10.0, vec2(levelSize));\n        if (levelAlpha > epsilon) {\n            if (uResolution < 2) {\n                discard;\n            }\n            gl_FragColor = vec4(vec3(0.7), levelAlpha);\n            return;\n        }\n\n        discard;\n    }\n",vertexWGSL:"\n    attribute vertex_position: vec3f;\n    attribute aUv0: vec2f;\n\n    uniform matrix_model: mat4x4f;\n    uniform matrix_viewProjection: mat4x4f;\n\n    varying uv0: vec2f;\n\n    @vertex\n    fn vertexMain(input: VertexInput) -> VertexOutput {\n        var output: VertexOutput;\n        output.position = uniform.matrix_viewProjection * uniform.matrix_model * vec4f(input.vertex_position, 1.0);\n        output.uv0 = input.aUv0;\n        return output;\n    }\n",fragmentWGSL:"\n    uniform uHalfExtents: vec2f;\n    uniform uColorX: vec3f;\n    uniform uColorZ: vec3f;\n    uniform uResolution: u32;\n\n    varying uv0: vec2f;\n\n    // https://bgolus.medium.com/the-best-darn-grid-shader-yet-727f9278b9d8#1e7c\n    fn pristineGrid(uv: vec2f, ddx: vec2f, ddy: vec2f, lineWidth: vec2f) -> f32 {\n        let uvDeriv = vec2f(length(vec2f(ddx.x, ddy.x)), length(vec2f(ddx.y, ddy.y)));\n        let invertLine = vec2<bool>(lineWidth.x > 0.5, lineWidth.y > 0.5);\n        let targetWidth = vec2f(\n            select(lineWidth.x, 1.0 - lineWidth.x, invertLine.x),\n            select(lineWidth.y, 1.0 - lineWidth.y, invertLine.y)\n        );\n        let drawWidth = clamp(targetWidth, uvDeriv, vec2f(0.5));\n        let lineAA = uvDeriv * 1.5;\n        var gridUV = abs(fract(uv) * 2.0 - 1.0);\n        gridUV.x = select(1.0 - gridUV.x, gridUV.x, invertLine.x);\n        gridUV.y = select(1.0 - gridUV.y, gridUV.y, invertLine.y);\n        var grid2 = smoothstep(drawWidth + lineAA, drawWidth - lineAA, gridUV);\n\n        grid2 *= clamp(targetWidth / drawWidth, vec2f(0.0), vec2f(1.0));\n        grid2 = mix(grid2, targetWidth, clamp(uvDeriv * 2.0 - 1.0, vec2f(0.0), vec2f(1.0)));\n        grid2.x = select(grid2.x, 1.0 - grid2.x, invertLine.x);\n        grid2.y = select(grid2.y, 1.0 - grid2.y, invertLine.y);\n\n        return mix(grid2.x, 1.0, grid2.y);\n    }\n\n    @fragment\n    fn fragmentMain(input: FragmentInput) -> FragmentOutput {\n        var output: FragmentOutput;\n        let uv: vec2f = input.uv0;\n\n        let pos: vec2f = (uv * 2.0 - 1.0) * uniform.uHalfExtents;\n        let ddx: vec2f = dpdx(pos);\n        let ddy: vec2f = dpdy(pos);\n\n        let epsilon: f32 = 1.0 / 255.0;\n\n        var levelPos: vec2f;\n        var levelSize: f32;\n        var levelAlpha: f32;\n\n        levelPos = pos * 0.1;\n        levelSize = 2.0 / 1000.0;\n        levelAlpha = pristineGrid(levelPos, ddx * 0.1, ddy * 0.1, vec2f(levelSize));\n        if (levelAlpha > epsilon) {\n            var color: vec3f;\n            if (abs(levelPos.x) < levelSize) {\n                if (abs(levelPos.y) < levelSize) {\n                    color = vec3f(1.0);\n                } else {\n                    color = uniform.uColorZ;\n                }\n            } else if (abs(levelPos.y) < levelSize) {\n                color = uniform.uColorX;\n            } else {\n                color = vec3f(0.9);\n            }\n            output.color = vec4f(color, levelAlpha);\n            return output;\n        }\n\n        levelPos = pos;\n        levelSize = 1.0 / 100.0;\n        levelAlpha = pristineGrid(levelPos, ddx, ddy, vec2f(levelSize));\n        if (levelAlpha > epsilon) {\n            if (uniform.uResolution < 1) {\n                discard;\n            }\n            output.color = vec4f(vec3f(0.7), levelAlpha);\n            return output;\n        }\n\n        levelPos = pos * 10.0;\n        levelSize = 1.0 / 100.0;\n        levelAlpha = pristineGrid(levelPos, ddx * 10.0, ddy * 10.0, vec2f(levelSize));\n        if (levelAlpha > epsilon) {\n            if (uniform.uResolution < 2) {\n                discard;\n            }\n            output.color = vec4f(vec3f(0.7), levelAlpha);\n            return output;\n        }\n\n        discard;\n        return output;\n    }\n",attributes:{vertex_position:o.JYe,aUv0:o.slc}}),this._material.blendType=i.i96,this._material.cull=o.pLF,this._material.update();const t=a.e.fromGeometry(this.app.graphicsDevice,new c.b);this._meshInstance=new l.F(t,this._material),this._meshInstance.pick=!1,this.entity.render.meshInstances=[this._meshInstance],this.colorX=this._colorX,this.colorZ=this._colorZ,this.resolution=this._resolution,this._set("uHalfExtents",this._calcHalfExtents(f)),this.app.on("prerender",()=>{if(!this.enabled)return;const t=this._calcHalfExtents(f);this._halfExtents.distance(t)>.001&&this._set("uHalfExtents",t)}),this.on("enable",()=>{this._meshInstance.visible=!0}),this.on("disable",()=>{this._meshInstance.visible=!1}),this.on("destroy",this.destroy,this)}_calcHalfExtents(t){const e=this.entity.getLocalScale();return t.set(e.x/2,e.z/2)}_set(t,e){this._material&&(e instanceof r.Q&&this._material.setParameter(t,[e.r,e.g,e.b]),e instanceof s.Z&&this._material.setParameter(t,[e.x,e.y]),"number"==typeof e&&this._material.setParameter(t,e),this._material.update(),this._meshInstance.material=this._material)}set colorX(t){t instanceof r.Q&&(this._colorX.copy(t),this._set("uColorX",this._colorX))}get colorX(){return this._colorX}set colorZ(t){t instanceof r.Q&&(this._colorZ.copy(t),this._set("uColorZ",this._colorZ))}get colorZ(){return this._colorZ}set resolution(t){this._resolution=t,this._set("uResolution",this._resolution)}get resolution(){return this._resolution}destroy(){this.entity.removeComponent("render")}}},8939(t,e,n){n.d(e,{X:()=>l});var r=n(93953),s=n(27351),o=n(53801),i=n(71586),a=n(85169);class l extends a.e{static scriptName="shadowCatcher";scale=new r.e(1,1,1);geometry;_geometryCreated=!1;initialize(){const t=new o.F;t.blendType=s.$M5,t.shadowCatcher=!0,t.useSkybox=!1,t.depthWrite=!1,t.diffuse.set(0,0,0),t.specular.set(0,0,0),t.update(),!this.geometry&&this.entity.render&&(this.geometry=this.entity),this.geometry||(this._geometryCreated=!0,this.geometry=new i.w("ShadowCatcherGeometry"),this.geometry.addComponent("render",{type:"plane",castShadows:!1,material:t})),this.geometry!==this.entity&&this.entity.addChild(this.geometry),this.geometry?.render?.meshInstances.forEach(e=>{e.drawBucket=250,this._geometryCreated||(e.material=t)}),this.on("destroy",()=>{this._geometryCreated&&this.geometry?.destroy(),t.destroy()})}update(){this.geometry?.setLocalScale(this.scale)}}}}]);